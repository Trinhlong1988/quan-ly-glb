# BUGS_FIXED — Quản Lý GLB

> Rule: mỗi bug do LEAD/AUDIT phát hiện = **thất bại của quy trình test** → BẮT BUỘC thêm test/rule chặn tái diễn trước khi đóng.
> Format: `### B<NN> — <mô tả> [FIXED|PENDING]` · Phát hiện bởi · Nguyên nhân · Fix · **Regression** (test/rule chặn tái diễn).

Counter: B = 80 (+ hardening P1-01/04/07, P2-02/03 đóng). PF (process-failure) = 10. Last ship: 2026-07-16 (**exe 0.2.58 — AUDIT-FIX 0.2.57 (audit ngoài) + icon: B77 Health-Scan chạy PRAGMA SQLite trên PG → nuốt lỗi im → BÁO OK GIẢ (gỡ 2 PRAGMA + `safe()` đẩy CHECK_FAILED + đếm ĐỘNG 11, không hard-code) · B78 backup chỉ ở máy chủ (A1: gate isServerRole backend+scheduler+khóa nút UI) · B79 đo ổ đĩa máy trạm sai (statfs chỉ khi server) · B80 icon taskbar không đồng bộ (setAppUserModelId khớp appId + set icon cửa sổ + đóng gói icon.ico) · B2 ký số HOÃN (chấp nhận SmartScreen). PF-10: test PASS ≠ check ĐÃ CHẠY — audit ngoài + engine-migration để lại check chết lặng; audit ngoài còn khẳng định SAI "git 0.2.34" (thực local+origin 0.2.57 tag đã push). Gate: typecheck 0 · 289 vitest · ST16 39/0 + ST17 26/0 + ST36 18/0 · 2 guard · **handtest ảnh máy chủ/máy trạm**. Icon CHỜ PV (nhìn taskbar thật). Trước 2026-07-16 (**exe 0.2.57 — DỌN NỢ HARDENING (Mr.Long "dọn nợ 2 3 4"): P1-01 fail-closed (dbReady gate IPC khi DB hỏng) · P1-04 verify sender (246 handler qua wrapper `handle()` + isTrustedSender) · P2-03 map lỗi PG (nuốt host/user, log ở main) · P2-02 magic-bytes (sniffFileType chống .exe đổi đuôi + fixture gcfg4/5) · P1-07 serial gần-trùng (normalizeSerial guard createPos) · P1-09 quyết định GIỮ tính-quyền-lúc-duyệt · Info-B quyết định số HĐ NHẢY QUÃNG được phép · #4 đính chính (Yêu cầu xuất kho đã build, không phải nợ)**; gate verify 289 vitest + typecheck web+node 0 · ST2/3/8/9/39-44 failures=0 · TEST TAY IPC (wrapper không chặn nhầm login/dashboard/data). **Bug class (process):** CẤM báo cáo "còn nợ X" chỉ dựa index memory cũ — PHẢI verify code hiện tại trước (đã bắt hụt #4). Trước 2026-07-16 (**exe 0.2.56 — Bill Giải Trình dọn nốt bug + nợ 1 lượt (Mr.Long "dọn nốt bug một lượt, nợ, rồi ship bản mới"): B74 bug SẢN XUẤT >500tr không sinh được → engine CÂN ĐỐI theo giá trị (loại SP 1-đơn-vị-đã->3×-phần-chia, ratio 3.6 → ≤3.5) + trần 299tr (service VALIDATION + CẢNH BÁO trên form bôi đỏ/chặn nút) không tách HĐ + B75 freeze ~30-50s treo main (deadline 300ms + hoist sort) + B76 float đồ-cân phá money-exact (Math.round mỗi dòng) + F1/F4 seed dedup ngành theo normIndustryName (case+NFD+space) + audit atomic bill+audit 1 tx**; gate verify 284 (vitest) · typecheck web+node 0 · ST44 failures=0 (thêm F4 NFD/space dedup + B-realistic cân đối≤3.5/qty≤200/no-freeze<1.5s/service-reject-300tr) · ST2 PASS · **TEST TAY playwright-electron (screenshot)**: form 350tr → viền đỏ + cảnh báo "Vượt trần 299tr" + nút Sinh bill CHẶN; 5tr → sạch + nút mở. Trước 2026-07-16 (**exe 0.2.55 — Bill Giải Trình rút-kinh-nghiệm + trả nợ kỹ thuật: B71 seed trùng ngành (case-sensitive) + B72 số lượng dòng phi lý (40 nồi cơm/100kg hành) + B73 guard target-quá-lớn treo UI + B1 dọn file mồ côi khi DB fail + FEATURE cột `priority` ưu tiên SP hữu dụng + A3 vitest thuần engine (11 test trong `npm run verify`)**; gate verify 283 (vitest) · typecheck web+node 0 · ST44 failures=0 (thêm A' priority round-trip + B-realistic + B71 dedup case-insensitive) · ST2 PASS. Trước 2026-07-16 (**exe 0.2.54 — AUDIT độc lập 0.2.53 (Codex/PING) Bill Giải Trình: B70 sửa BILL-01/02/03/04/05/06/09 (strict tiền/ngày/ngành/TID + số HĐ atomic advisory-lock) + FE53-01..06 (try/catch/finally, gửi chuỗi raw) + REJECT BILL-08 + version-sync + npm selftest script**; tái dựng `npm run selftest:bill` — ST44 ~55 assert (gồm 25 ca đối kháng malformed + concurrency Promise.all) failures=0 · vitest 272 · typecheck web+node 0. Trước 2026-07-16 (**exe 0.2.53 — MENU "Bill Giải Trình" (port renbill) 3 tab + seed thư viện GỐC renbill 1 lần/DB (5 ngành + 199 SP Siêu thị thật) + B69 header-bảng-bị-nút-đè-khi-cuộn (z-20 cho 39 thead/28 file)**; gate typecheck web+node 0 · ST44 PASS (CRUD+import+money-exact+degrade+seed+guard quyền) · ST2 PASS · guard 2230 dòng 5 anchor · **TEST TAY playwright-electron+screenshot**: menu render, z-index thead computed=20, Siêu thị 199 SP, sinh bill qua UI "Đã sinh 2 bill"+file thật — chi tiết B69 dưới). Trước 2026-07-16 (**exe 0.2.52 — B68 mất-admin-cuối qua updateUser + gói M-2/M-3/L-4/L-5 + Bug #1 search-jump Khách/GD/Hồ sơ**; gate typecheck 0 · vitest 272 · ST2 PASS gồm 4 regression H-1/M-2 · ST15 108/0 — chi tiết cuối file). Trước 2026-07-14 (**exe 0.2.35 FIX DB-tiến-hóa login + UI**. **B67 (login 0.2.34 "Lỗi hệ thống khi đăng nhập")**: bản 0.2.34 (PostgreSQL, dòng D:) login CRASH trên production `glb` vì DB thiếu cột `users.lock_reason` — cột thêm ở P0-01 (`auth-service` login đọc `row.lockReason`), nhưng Prisma 7 (`prisma-client` queryCompiler) KHÔNG có migrate engine trong .exe → deploy .exe mới mà QUÊN `prisma migrate deploy` trên DB dùng chung = login query `SELECT ... lock_reason` → "column does not exist" → sập. Phát hiện: session trước (report DESKTOP_SHIP_AUDIT) — đã vá 1 DB thủ công `ALTER TABLE users ADD COLUMN lock_reason`. FIX TRIỆT ĐỂ (không chỉ 1 DB): `ensureCriticalSchema(db)` trong `initDb` chạy MỖI boot (server+client, non-fatal) `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` cho `users.lock_reason` + `export_requests.method` → fresh DB no-op, DB cũ tự thêm cột → deploy KHÔNG cần migrate tay vẫn login được. Regression ST35 §0: DROP cột → login NÉM đúng "column users.lock_reason does not exist" → `ensureCriticalSchema` → login OK. Bug class: **cột/quyền ADDITIVE mà code ĐANG ĐỌC phải self-heal khi boot (ADD COLUMN IF NOT EXISTS) — Prisma-7-no-migrate-engine + deploy-quên-migrate = crash; đừng dựa vào migrate tay trên mọi DB.** Nối [[feedback_verify_before_claim_and_db_upgrade_gap_9_7]] + B53. + UI (Mr.Long 14/7): đồng hồ realtime topbar (Intl HCM+cleanup) · sidebar navy đậm hơn + menu 3D · tìm kiếm toàn cục (global-search-service permission-gated + dropdown debounce, ST15 §M) · "danh sách lệnh duyệt đã xóa" (bill+entity APPROVED, DTO +decidedByName/At/Note). Đối chứng bug Codex (C: SQLite 0.1.0): B10-B16 D: đã bao phủ hết (report DESKTOP_SHIP_AUDIT). Gate typecheck 0·build 0·vitest 266·FULL ST2-43=0. Trước — **exe 0.2.33 PING audit đợt 2 — ĐÓNG NỐT 8/8 invariant**. **B64 (P1-02)** serverConfig lộ password DB ra renderer: `currentServerConfig()`/`getServerConfig()` trả cả `password` → renderer prefill ô mật khẩu (secret rời main, lộ qua XSS/devtools). Fix: `getServerConfig` trả `password:''` + `passwordSet:boolean`; `fillPasswordFromStored` — blank khi save/test = giữ mật khẩu đã lưu (sửa host không phải gõ lại). Regression ST22 §11/§12. Bug class: **cấu hình nhạy cảm ra renderer chỉ được lộ CỜ (đã-đặt), không lộ giá trị secret.** **B65 (P1-05)** ngày nghiệp vụ cuộn âm thầm: `parseDate` dùng `new Date('2026-02-31')` → JS cuộn sang 03-03 (không NaN) → lưu sai tháng; `2026-13-01`→năm sau. Fix: kiểm Y-M-D round-trip UTC cho chuỗi date-only/ISO-Z → ngày không tồn tại = null = VALIDATION (2028-02-29 nhuận vẫn hợp lệ; 2026-02-29 không nhuận bị chặn). Regression ST15 A2. Bug class: **ngày nghiệp vụ phải là ngày CÓ THẬT — cấm để `new Date` cuộn ngầm sang tháng/năm khác (invariant #8).** **B66 (P1-08)** xóa entity còn quan hệ sống → mồ côi: `deleteCustomer` + entity-cancel Customer/TID xóa mềm KHÔNG kiểm quan hệ → khách đang giữ máy/TID/cọc, TID đang gắn máy vẫn xóa được → tham chiếu mồ côi. Fix: `customerLiveRelationGuard` (giữ máy POS / TID đã giao / cọc OPEN) dùng chung deleteCustomer + entity-cancel Customer.precheck; TID precheck chặn posSerial≠null / cọc OPEN / nằm trong phiếu xuất PENDING; RE-GUARD lại TRONG tx lúc DUYỆT (chống quan hệ phát sinh giữa yêu cầu→duyệt). Regression ST34 P1-08/P1-08b (+7). Bug class: **xóa (kể cả xóa mềm) entity có tham chiếu sống PHẢI chặn ở CẢ lúc yêu cầu LẪN lúc thực thi (re-guard in-tx) — invariant #7 chống mồ côi.** **8/8 invariant tài liệu PING đều có FIX + regression** (bảng E2 báo cáo). DEFERRED còn (hardening, không vi phạm invariant): P1-01 fail-closed initDb, P1-04 sandbox, P1-07 serial norm, P1-09 snapshot quyền requester, P2-02 upload, P2-03 map lỗi PG. Gate typecheck 0·build 0·vitest 253·FULL ST2-43=0. Trước — **exe 0.2.32 PING audit — 7/7 P0 bảo mật/logic + P1-03 + P2-01**. **B57 (P0-01)** khóa TAY của Admin tự mở sai policy: `setUserLock` không quản `lockedAt`; sau auto-lock cũ (lockedAt=T1) → admin mở→khóa lại → login (`auth-service #1`) tự mở vì lockedAt cũ >15′. Fix: schema `lockReason` (AUTH_FAILURE|ADMIN_LOCK, migration 20260714120000 backfill theo lockedAt) — login CHỈ tự mở khi lockReason=AUTH_FAILURE; khóa tay set ADMIN_LOCK+lockedAt null. Regression ST35 §13/§13b. Bug class: **cờ trạng thái khóa phải mang LÝ DO tường minh — không suy ra "được tự mở" từ 1 mốc thời gian dùng chung.** **B58 (P0-02)** race bộ đếm login sai: `failedAttempts+1` read-modify-write → 2 login sai song song ghi đè mất lần → chậm khóa. Fix: `updateMany increment:1` atomic + conditional transition LOCKED (chỉ 1 luồng count>0). Regression ST35 §14. Bug class: **bộ đếm bảo mật = atomic increment + conditional transition, cấm read-modify-write.** **B59 (P0-03)** duyệt hủy BILL không verify mật khẩu: `approveCancelBill(id,note)` KHÔNG nhận password; `approveOne` ghi chú "Admin tự duyệt (đã nhập mật khẩu)" nhưng KHÔNG verify (entity-cancel thì có). Fix: thêm `actorPassword` + verifyActorPassword (rỗng→VALIDATION, sai→WRONG_PASSWORD) cho cả bulk; wire IPC/preload/ApprovalPage (ApprovePasswordModal). Regression ST18 P0-03. Bug class: **note khẳng định hành vi bảo mật (đã nhập mật khẩu) thì service PHẢI thực thi — không tin renderer/ghi chú.** **B60 (P0-04)** reject hủy bill không kiểm transition: `rejectOne` hoàn bill CANCEL_PENDING→POSTED không kiểm `count` → nếu bill lệch trạng thái, Approval REJECTED nhưng bill giữ nguyên → LỆCH. Fix: `if (billMoved.count!==1) throw INVALID_STATE` (rollback cả REJECTED). Regression ST18 P0-04. Bug class: **chuyển trạng thái đối-xứng (approve/reject) phải kiểm count đối-xứng.** **B61 (P0-05)** hình thức thanh toán lạ âm thầm→CASH: `method==='CK'?'CK':'CASH'` → 'BANK'/typo thành tiền mặt (sai dòng tiền). Fix: allowlist `PAYMENT_METHODS` (trim+upper rồi VALIDATION nếu lạ). Regression ST43 Ca6c. Bug class: **normalize dữ liệu TÀI CHÍNH phải allowlist + fail, không ép ngầm.** **B62 (P0-06)** tiền qua Number trước BigInt: `toVnd` dùng `Number(v)` trung gian, không chặn >MAX_SAFE → làm tròn ngầm. Fix: parse chuỗi-chữ-số thẳng BigInt (regex `^\d+$`) + reject >MAX_SAFE_INTEGER. Regression ST43 Ca6d. Bug class: **tiền BigInt: input chuỗi→BigInt trực tiếp, cấm Number trung gian ở biên.** **B63 (P1-03)** "ghi nhớ đăng nhập" trả plaintext password về renderer: `getRemembered()` trả `{username,password}` (mâu thuẫn comment "never leaves main") → password vào React state (lộ qua XSS/devtools). Fix: `getRemembered→{username}` only + `auth:loginRemembered` giải mã trong main + login, renderer không thấy password. Bug class: **secret KHÔNG đi từ main sang renderer — đăng nhập-đã-nhớ do main tự thực hiện.** + **P0-07** audit vào $transaction ở approve/reject bill + approve export (atomic dấu vết). + **P2-01** memoize quyền requester (hết N+1 listCancelRequests). + **PF-09 "stale-build-che-test"**: Ca6b (0.2.31) thêm SAU `npm run build` → `run_all.sh` chạy `out/` build cũ → false-pass → ship 0.2.31 kèm assertion SAI (deliveredAt cho TID bán-kèm, đúng ra SOLD); chỉ lộ khi rebuild sạch. Đổi quy trình: **selftest PHẢI rebuild ngay trước khi chạy; DB selftest chỉ tính khi build từ HEAD.** Gate: typecheck 0·build 0·vitest 253·FULL ST2-43=0. Báo cáo `reports/PING_GLOBEWAY_FIX_REPORT.md`. P1/P2 còn lại (P1-01/04/05/07/08/09, P2-02/03, money-string-toàn-tuyến) phân loại DEFERRED chờ Mr.Long xếp lịch. Trước — **exe 0.2.31 CK + Bán kèm/rời TID cho Yêu cầu xuất kho** — FEATURE, 0 bug sản phẩm. Mr.Long 14/7 "thêm chuyển khoản, bán máy cũng cần gắn tid hoặc không gắn tid bán rời máy": (1) cột `method` CASH|CK cho export_requests (migration additive default CASH, đã áp glb) → form thêm ô "Hình thức thanh toán"; method chảy qua create→approve→bookSaleCashEntries/applyHandover/openDeposit → mọi CashEntry ghi đúng CK/CASH; badge CK ở danh sách phiếu. (2) MỞ withTid cho POS BÁN (SALE) — trước UI khóa `setWithTid(false)` cho SALE (backend đã sẵn processPosLineTx bán máy + bán kèm TID); nay Bán chọn "Bán rời (chỉ máy POS)" / "Kèm TID (gán khi duyệt)". Regression ST43 **Ca6b**: POS SALE kèm TID + CK → máy SOLD + TID delivered + DT +3tr + quỹ +3tr + method round-trip DTO + CashEntry.method=CK. Bug class phòng ngừa: **field nghiệp vụ mới (method/withTid) khi backend đã hỗ trợ mà UI khóa cứng → phải verify UI mở đủ + regression phủ round-trip DTO + bút toán tiền, không chỉ engine.** Gate typecheck 0·build 0·vitest 253·FULL ST2-43=0 fail. Trước — **exe 0.2.27 hồi sinh máy khi import lại serial đã xóa**. **B56** (Mr.Long "xóa hàng loạt máy rồi import lại: không hiện danh sách, chỉ tab nhập kho, Cài APP máy trắng"): `createPosIntake` `posDevice.findUnique({where:{serial}})` KHÔNG lọc deletedAt → import lại serial đã xóa mềm → nhánh "existing device" chỉ cập nhật cột nhập, KHÔNG bỏ deletedAt + KHÔNG set bankId → máy giữ trạng thái xóa (ẩn ở Danh sách máy) + máy trắng; phiếu nhập tạo mới → hiện tab nhập kho (khớp triệu chứng). Fix: isResurrect (máy tồn tại + deletedAt!=null) → bỏ chặn trùng serial + cập nhật phiếu nhập cũ (un-delete, không tạo trùng) + hồi sinh máy (deletedAt null/IN_STOCK/kho/Cài APP mới/xóa gán cũ). Regression ST29(e) verify DB. Bug class: **truy vấn "tồn tại theo khóa" trước khi update PHẢI xác định rõ ý định với record xóa mềm — trộn record đã xóa vào nhánh update sinh trạng thái ẩn/mồ côi.** GHI CHÚ: "chỉ nhận EIB" (serial mới) fix ở 0.2.24 (bankApp), "box tìm dialog giao dịch" có ở 0.2.23 (SearchSelect) — user báo đang chạy exe cũ (version drift). Gate: vitest 253 + FULL ST2-42=0 fail. Trước — **exe 0.2.24 Admin tự-duyệt + fix import Cài APP**. **B55** (Mr.Long "sao admin không được duyệt?"): hàm `list` (entity-cancel + bill) tính `canApprove=false` cho phiếu isSelf mà BỎ QUA fallback 1-Admin trong backend approveOne → UI ẩn nút Duyệt dù backend cho phép (khi 1-admin). Sâu hơn: glb có 3 admin nên adminroot tự tạo phiếu → không tự duyệt được (đúng luật 2-người) nhưng Mr.Long muốn Admin tự duyệt. Chốt: **Admin (elevated) được tự duyệt phiếu của mình, chốt = nhập mật khẩu** (canApprove=approverElevated, bỏ điều kiện count===1; Manager vẫn không). B54(0.2.23) chỉ hiện phiếu tự-tạo; B55 cho Admin DUYỆT nó. Bug class: **cờ hiển-thị-nút (canApprove) phải KHỚP logic-quyết-định thật ở backend, nếu không nút biến mất dù thao tác được.** + Import Cài APP khớp rộng (mã/tên-ngắn-bỏ-tiền-tố/"Máy trắng" — resolver bankApp mới; Mr.Long "có NH load được có NH không/báo trống" do trước chỉ nhận mã+tên-đầy-đủ). Selftest #31 +6 ca. Gate: vitest 253 + FULL ST2-42=0 fail. Trước — **exe 0.2.23 phiếu-hủy-tự-tạo hiện + redesign form doanh thu**. **B54** (Mr.Long "đã yêu cầu hủy máy pos/tid nhưng trong quản lý duyệt hủy không cập nhật"): trang Duyệt hủy `ApprovalPage` lọc `.filter(r => r.canApprove)` → phiếu do CHÍNH người xem tạo có `canApprove=false` (chặn tự-duyệt R34: isSelf→false) → **ẩn hoàn toàn** → người tạo không thấy phiếu mình đang chờ (và nếu là admin duy nhất thì tưởng mất). Fix: thêm `isSelf` vào EntityCancelRequestDto + CancelRequestDto (backend đã tính isSelf, chỉ chưa expose); renderer hiện `canApprove || isSelf`, phiếu tự-tạo hiện nhãn "Chờ người khác duyệt" + ẩn nút Duyệt. Bug class: **UI ẩn bản ghi theo cờ quyền-hành-động (canApprove) làm MẤT khả năng THẤY bản ghi — phải tách "thấy" khỏi "thao tác được"; verify bằng live-test vì gate backend/vitest trả DTO đúng nhưng render lọc sai.** Nối [[feedback_ui_test_gap_paired_qa_loop_10_7]]. Kèm redesign form Ghi nhận giao dịch (#5-11: format tiền, show biểu phí khi chọn loại phí, khách tự-link theo TID, thêm giờ, modal to, cascading NH→HKD→TID, combobox tìm kiếm riêng SearchSelect) + migration export_request (2 bảng rỗng Phase 1). Gate: vitest 253 + FULL ST2-42=0 fail. Trước — **exe 0.2.22 UI polish + Cài APP lan tỏa** — 0 bug backend. "Tràn chữ" cột Thao tác (Mr.Long ảnh chụp): bảng POS quá rộng sau khi thêm cột Cài APP → nút thao tác bị cắt mép phải → fix nút chỉ-ICON (`IconAction`, hover chữ to) + font bảng −5% + nowrap cuộn ngang. Đây là **lỗi TẦNG RENDER, gate backend/vitest KHÔNG thấy** (nối [[feedback_ui_test_gap_paired_qa_loop_10_7]] — chỉ Mr.Long live-test/ảnh chụp mới bắt được overflow layout). Cài APP lan tỏa: cột danh sách nhập kho + import Excel (để trống=máy trắng). Menu: Máy POS→TID→Doanh thu; "Quản Lý Ngân Hàng - Đối Tác" xuống dưới Tài Chính. KHÔNG migration. Gate: vitest 253 + FULL ST2-42=0 fail. Trước — **exe 0.2.21 Cài APP + luật gán TID trùng bank** — FEATURE, 0 bug sản phẩm. Máy POS thêm trường "Cài APP (ngân hàng)" (dùng lại cột bankId sẵn có, KHÔNG migration) + LUẬT gán TID chỉ khi bank TID trùng app máy (máy trắng→MACHINE_BLANK, khác bank→BANK_MISMATCH; áp assignTid/createTidUnified/replaceTid). **PF-08**: luật gán mới đổi PRECONDITION của assignTid → phá 8 selftest cũ (máy tạo không app + TID trần không bank → MACHINE_BLANK/BANK_MISMATCH hàng loạt). LEAD lường trước, giao subagent thread bankId khớp per-assignment (posunify/tidunify/poslifecycle/gpos/devicesale/handover) + thêm ca luật mới, rồi TỰ chạy FULL ST2-42 = 0 fail xác minh. Bài học: **thêm ràng buộc nghiệp vụ lên 1 thao tác lõi = đổi precondition của MỌI test gọi nó → phải quét hết caller trong fixture + rerun full DB; đừng tin gate vitest.** Nối [[feedback_subagent_must_run_db_selftest_not_just_gate]]. Tab "Danh mục kho"→"Danh sách kho" ra ngoài cùng + fix cột Thao tác đè chữ (nowrap). Trước — **exe 0.2.20 "ship cả thể"** — FEATURE cụm tiền, 0 bug sản phẩm mới nhưng **2 process-failure**: **PF-06** cụm loại phí build SAI mô hình lần đầu (cho loại phí đổi cả phí mua/cài/bán) → Mr.Long chỉnh "Ủy quyền/Tiền chờ/Tiền nhanh CHỈ liên quan phí bán ra" → rework: phí mua/cài CỐ ĐỊNH + phí bán niêm yết theo loại phí (FeeSellQuote) + set phí bán TID chọn niêm yết/tùy chỉnh. Bài học: **spec mô hình tiền phải chốt bằng ví dụ số cụ thể TRƯỚC khi build; đừng suy diễn "loại phí = mọi phí đổi" khi user chỉ nói phí bán.** **PF-07** selftest-warehouse §4 dùng username 'whmgr01' 7 ký tự < tối thiểu 8 → createUser VALIDATION → 4 assert địa chỉ/SĐT null theo (TÁI lớp B26) → LEAD full-selftest ST40 bắt (subagent chỉ chạy vitest, KHÔNG chạy selftest DB nên không thấy). Fix username ≥8. Bài học đã ghi memory [[feedback_subagent_must_run_db_selftest_not_just_gate]]: **LEAD LUÔN tự chạy full ST DB sạch trước ship; test data phải hợp lệ theo validator app.** Cụm ship: loại phí sell-only + loại giao máy (HandoverType Bán/Cho thuê/Mượn/Cọc: RENT→doanh thu, DEPOSIT→cọc nợ phải trả + recall hoàn cọc, NONE→0đ, SALE→device-sale) + Nhóm 1 UI (Sửa full POS/TID + tích chọn xóa hàng loạt qua Duyệt Hủy + tab kho + kho gán user + giao khách bắt buộc kho có địa chỉ) + fix locale ngày dd/mm. 3 migration áp glb (backup glb_pre_0.2.20). Gate: vitest 253 + FULL ST2-42 = 0 fail (ST42 HANDOVER mới). Trước — **B53** production, Mr.Long "chưa thấy menu kho": DB `glb` thiếu 4 quyền mới CONFIG_WAREHOUSE_*/DEVICE_SALE_* (87/91) → menu ẩn cả với ADMIN. Gốc: seedIfEmpty (upsert catalog quyền từ PERMISSION_CODES + R_ADMIN_SUPERUSER mỗi boot) CHỈ chạy vai server; app cài thường chạy vai client → auto-update KHÔNG mang quyền mới vào DB dùng chung → quyền thêm sau lần seed đầu không bao giờ tới production. Fix: (a) seed tay 4 quyền + 15 gán role (ADMIN/MANAGER cả 4, D_MANAGER/ACCOUNTANT/WAREHOUSE theo DEFAULT_ROLE_PERMISSIONS) vào glb ngay; (b) db.ts initDb cho CLIENT cũng chạy seedIfEmpty khi boot, bọc pg_advisory_lock(918273645) serialize đa-client + NON-FATAL (lỗi seed client không sập app) → quyền mới tự đồng bộ khi bất kỳ máy nào cập nhật. Regression: permission-catalog.rules.test.ts (mọi mã DEFAULT_ROLE_PERMISSIONS ∈ PERMISSION_CODES) + selftest server-role đã phủ catalog completeness. Bug class: **catalog quyền phải đồng bộ vào DB dùng chung ở CẢ vai client (idempotent+advisory-lock), không chỉ server — nếu không, feature mới ẩn âm thầm trên production sau auto-update.** Kèm exe 0.2.19: Vòng đời tiếng Việt + note Khách/Kho/Người-thực-hiện + đổi cột TID (Ngân hàng cạnh HKD). Gate: vitest 253 + ST2-41 0 fail. Trước — TỔNG KIỂM TRA ĐỐI KHÁNG (exe 0.2.17, Mr.Long "check sâu bug ẩn/logic + 100% pass + dọn rác + khóa + đồng bộ repo"): 3 agent (logic-reviewer + security-auditor R48 + dead-code sweep) → LEAD tự VERIFY từng finding tại code trước fix. 5 bug thật: **B48** (CRITICAL tiền) hủy phiếu THU bán thiết bị SALE_COLLECT chỉ hoàn cashDebtSettlement mà KHÔNG hoàn deviceSaleSettlement → quỹ giảm nhưng công nợ mua thiết bị kẹt (remaining sai) + chặn thu lại → fix cancelCashEntry deleteMany deviceSaleSettlement theo cashEntryId. **B49** deleteWarehouses không chặn kho còn máy IN_STOCK → xóa kho → máy mắc kẹt (giao lại NOT_FOUND) → fix đếm posDevice>0 → IN_USE. **B50** user:create dùng me() in-memory bỏ qua validateCurrentSession → bypass forceChangePassword/TTL/khóa → fix route qua session guard. **B51** file:open truyền path tùy ý vào shell.openPath → RCE (.exe/.bat) qua XSS → fix chỉ mở .xlsx do app xuất trong phiên. **B52** file:read gác bằng mã quyền DOSSIER_VIEW/RCV_ACCT_VIEW KHÔNG tồn tại (đúng CONFIG_*) → fail-CLOSED chặn cả ADMIN, tính năng chết âm thầm → fix đúng mã + window-open lọc http/https. Bác 4 finding phụ có lý do (thu-nợ-không-mật-khẩu nhất quán createDebtReceipt; pre-read TOCTOU-safe qua updateMany; snapshot kho chỉ hiển thị; serverConfig thick-client vốn giữ creds). Regression: #41 +Ca9/Ca10 + **vitest guard ipc-permcode** (quét tĩnh mọi mã quyền requirePermission ∈ PERMISSION_CODES → chặn lớp fail-closed âm thầm). Dọn 8 dead-code (0 ref repo). Bug class: **hủy chứng từ tiền phải hoàn TẤT CẢ đối soát phát sinh từ nó; xóa master-data phải chặn khi còn con trỏ tới; mã quyền IPC phải có guard kiểm tồn tại (fail-closed = mất tính năng âm thầm).** Full ST2-41 = 0 fail sau dọn rác. Trước — SIẾT CỨNG BACKEND thu hồi/nhận-sửa BẮT BUỘC kho (exe 0.2.16, Mr.Long "ok lfm đi"): recall/receiveRepaired thiếu toWarehouseId → VALIDATION ở backend (không chỉ UI); state-aware qua decidePosTransition(...).allowed để KHÔNG che INVALID_STATE (gpos DEPLOYED→receiveRepaired vẫn INVALID_STATE, không thành "thiếu kho"). 5 selftest sửa (process failure → update test): gpos/posunify/tidunify/poslifecycle/devicesale tạo kho + truyền toWarehouseId; #39 +ca "thu hồi không kho→VALIDATION". Bug class: **ràng buộc bắt buộc phải enforce ở BACKEND, UI chỉ là lớp tiện; validation phải state-aware để không che lỗi trạng thái.** Trước — KHO MÁY POS Model 1 + đồng bộ + fix tên backup (exe 0.2.15) — FEATURE + 1 fix ẩn: (1) PosDevice.warehouseId = kho vật lý đang chứa máy (nguồn sự thật duy nhất, bất biến warehouseId≠null ⟺ IN_STOCK); set khi nhập kho/thu hồi-về-kho, xóa null khi rời kho (giao/bán/thanh lý/hỏng/gửi-sửa) tại applyTransition + sellPos. (2) ĐỒNG BỘ (Mr.Long "tất cả liên quan đến dữ liệu kho phải đồng bộ"): deploy fromWarehouseId + sellPos sale.warehouseId TỰ lấy dev.warehouseId (không cho chọn lệch), UI read-only khi máy trong kho; thu hồi/nhận-sửa bắt chọn Về-kho (kho đích tồn tại→NOT_FOUND). (3) Lọc "Kho" + cột Kho + Excel. (4) FIX ẩn: backupFileName trùng trong cùng GIÂY (2 backup/giây đè tên nhau → mất bản) → thêm millisecond + token ngẫu 4 ký tự, sort vẫn theo thời gian; test 50 lần cùng thời điểm ra 50 tên. Migration pos_warehouse. Regression #39 +Model-1 (deploy rời kho/thu hồi về kho/đồng bộ fromWarehouse/lọc/kho treo) + #41 sell-clears-kho + backup.rules 5/5. Bug class: **1 thực thể = 1 nguồn sự thật vị trí kho; mọi màn đọc từ đó, cấm nhập lệch. Tên file phái sinh thời gian phải đủ mịn (ms+random) chống trùng.** Trước — DỌN 3 NỢ KỸ THUẬT "dọn nợ rồi ship" — 0 bug mới, hạ nợ tồn: #1 gỡ dead code sửa GD: TransactionForm nhánh mode:'edit' + wire transactionUpdate (IPC/preload/d.ts/UpdateTransactionInput) KHÔNG có caller (chỉ create) → gỡ, form CREATE-ONLY; GIỮ service updateTransaction vì là guard BILL_IMMUTABLE (selftest #15/#18 gọi trực tiếp). #2 SAO LƯU TẦNG 2 (R48 Pha 5, đóng nợ #49): mirror off-box sau mỗi backup tại choke-point writeBackupArchive (phủ mọi đường: thủ công/tự động/pre-restore/pre-autofix) + verify size + rotation giữ N + audit BACKUP_MIRRORED/FAILED + báo Admin, mirror lỗi NON-FATAL (không hỏng backup gốc); config AppSetting + UI tab Sao lưu (gate BACKUP_RESTORE + mật khẩu + probe); regression #36+C6 16/16 (bật/nhân bản/rotation/lỗi non-fatal). #3 badge SOLD: migration status_sold builtin POS_DEVICE/SOLD/Đã bán/violet + StatusPill SOLD map + dashboard label. Bug class: **backup PHẢI có bản off-box (1 ổ hỏng ≠ mất data); mirror lỗi phải NON-FATAL + báo động, không im lặng.** Trước — Bán máy/TID + Công nợ mua thiết bị "gop tron" — FEATURE, 0 bug mới: cluster bán thiết bị + công nợ + hủy khách. Backend `device-sale-service` (sellPos/sellTid/collect/list/receivables) trong $transaction+withRetry+FOR UPDATE; doanh thu accrual (CashEntry SALE_POS/SALE_TID fundId=null vào doanh thu ngay) tách bạch thu tiền mặt (SALE_COLLECT affectsPnl=false + DeviceSaleSettlement vào quỹ) — danh mục riêng để KHÔNG lẫn công nợ POS DEBT_CUSTOMER (đã verify POS debt dùng CashDebtSettlement per-transaction, không tổng theo khách → không nhiễm). Bán máy KÈM TID (TID→SOLD+unbind), hủy khách giữ recallPending để thu về. Quyền TIỀN riêng DEVICE_SALE_VIEW/MANAGE (không mượn POS/TID_MANAGE — "làm đúng vai trò"); DB-evolution grant role cũ + cờ; danh mục SALE_COLLECT seed khi boot. Migration `20260712210000_device_sale` áp glb (backup glb_pre_device_sale_20260712_145252.dump). UI: PosPage Bán máy + SellDeviceModal + menu Hủy khách + badge/lọc Cần thu hồi; TidPage Bán TID + SellTidModal; tab "Công nợ mua thiết bị" (nhóm khách → chi tiết đơn → Thu tiền, chặn thu vượt nợ). Regression selftest #41 DEVSALE 34 assert money-exact (bán 2tr thu 2tr → DT+2tr quỹ+2tr; bán 3tr thu 0 → DT+3tr công nợ+3tr; thu nợ KHÔNG cộng DT). Bug class MỚI đóng: **thao tác tiền = quyền tiền riêng (không mượn quyền nghiệp vụ); doanh thu accrual phải tách danh mục CashCategory với thu tiền mặt để không đếm trùng.** Trước — R27 Kho + quét nợ kỹ thuật: + R27 Danh mục Kho (feature): entity Warehouse master-data (mã/tên/địa-chỉ/đt/trạng-thái) CRUD+optlock+soft-delete+quyền CONFIG_WAREHOUSE_*+DB-evolution grant role cũ; giao/đổi-khách dropdown "Từ kho" → chọn kho hiện địa chỉ, AssetEvent ghi fromWarehouseId + SNAPSHOT deliveryAddress (lịch sử không đổi khi kho sửa địa chỉ); menu mới; selftest #40. + QUÉT NỢ KỸ THUẬT toàn repo (agent read-only): (1) code sạch 0 TODO/FIXME thật; (2) nghi vấn "gap DB-evolution thiếu grant BILL_CANCEL/R34/CONFIG_* cho role cũ" → VERIFY glb thật: MANAGER/ACCOUNTANT đã đủ quyền (seed sau feature) → KHÔNG bug (bài học verify-before-claim); (3) dead-code mode:'edit' TransactionForm unreachable → HOÃN gỡ (trang tiền không có test render UI, tránh rủi ro, chờ Mr.Long duyệt commit riêng); (4) test class "DB tiến hóa" (nợ đã khai BUGS_FIXED cũ) → đóng 1 phần qua selftest #40 grant-to-existing-roles. Full suite 38/38 ALL GREEN (ST2-40). Bug class mới đóng: DB-evolution phải có test mô phỏng role-cũ-thiếu-quyền → grant → có (selftest #40 chuẩn hóa). Trước — Vòng đời POS #1+#2 (Mr.Long duyệt "oik" 12/7): + POS #1 HARDENING (backstop DB cho bất biến 1 máy 1 TID SỐNG): trước đây bất biến chỉ enforce bằng guard tầng service (assignTid TID_ON_DEVICE + DEVICE_HAS_TID + FOR UPDATE) — KHÔNG có chốt DB, nên bất kỳ đường ghi lỗi/raw nào cũng có thể đẻ 2 binding mở/máy hoặc 1 TID sống trên 2 máy → migration `20260712190000_pos_binding_unique` thêm 2 partial-unique `UNIQUE(pos_serial) WHERE unbound_at IS NULL` + `UNIQUE(tid) WHERE unbound_at IS NULL`; selftest #39 chèn raw binding thứ 2 → 23505 (chứng minh chốt DB). + POS #2 (feature) sự kiện đổi-khách atomic `changeCustomer` (DEPLOYED→DEPLOYED giữ TID, TID theo khách mới, AssetEvent CHANGE_CUSTOMER ghi kèm TID + audit, 1 transaction khóa tids→pos_devices), UI menu + banner, chặn trùng/thiếu/không-tồn-tại/sai-trạng-thái. Regression selftest #39 POSLIFE 22 assert 0 fail; full suite 37/37 ALL GREEN. Bug class: mọi bất biến "duy nhất theo điều kiện" (1 mở/khóa) PHẢI có partial-unique DB backstop, KHÔNG chỉ guard service. Trước — R48 Pha 4 realtime + gọn bảng TID: + realtime giữa các máy (feature): bảng `change_tokens` bump version trong `writeAudit` (choke-point) → `realtimeTokens()` poll ~10s: badge số yêu-cầu-chờ-duyệt trên menu "Duyệt hủy" + thanh "Dữ liệu vừa được người khác cập nhật — Tải lại" trên 11 trang; Provider chỉ chạy khi đã đăng nhập; migration thêm bảng (backup glb trước); regression selftest-realtime #38 (8/8: version tăng đúng miền, không nhiễu chéo, pendingCancels hợp lệ). + B47 tên HKD trong bảng TID bị CẮT 2 dòng (HKD/Ngành/Ngân-hàng/Khách-giữ wrap) → thêm whitespace-nowrap 1 hàng + cột "Thao tác" icon-only (tooltip) nhường bề ngang dữ liệu — Mr.Long production 12/7. Full suite 36/36 ALL GREEN. Trước — R48 Pha 3b optimistic-lock: + B46 SỬA ĐÈ IM LẶNG (lost update): 2 người mở cùng 1 bản ghi, người lưu SAU ghi đè mất thay đổi của người lưu TRƯỚC, không ai biết → phủ HẾT 21 form Sửa: client giữ `updatedAt` lúc mở, gửi lại `expectedUpdatedAt` khi Lưu; backend `staleGuard` so mốc, lệch → `STALE_WRITE` (không đè) + client báo "Dữ liệu đã thay đổi" + tải lại. Tương thích ngược: không gửi mốc = không kiểm (call cũ/nội bộ không vỡ). 20 DTO lộ updatedAt (17 qua AuditTrail, 3 phẳng). Regression: selftest-optlock #37 (customer đủ kịch bản: mốc cũ→STALE, mốc mới→ok, không mốc→ok, mốc rác→bỏ qua) + industry/fund (2 kiểu DTO). Bug class: **mọi update entity qua form PHẢI kiểm optimistic-lock; mọi DTO sửa-được PHẢI lộ updatedAt.** + 3 bộ lọc mới (không phải bug — feature Mr.Long 12/7, có regression: ST8 +4 lọc TK theo mã/tên KH, ST30 +3 lọc TID theo khách-giữ/nguồn-hồ-sơ). Trước — R48 Pha 3a toàn vẹn: + B45 doanh thu ÂM lọt: `setFeeRate`/`setTidSellFees` chấp nhận phí bán/mua < phí cài máy → CL_KH âm → công nợ/doanh thu âm ngầm → chặn phiMua≥phiCaiMay + phiBan≥phiCaiMay ở CẢ biểu phí niêm yết LẪN phí bán thực tế theo TID; + B44 cửa sổ GD "nửa vời" khi crash: `createTransaction` create GD → (crash) → chưa kịp gán mã `GD00001`/chưa kịp ghi audit; các thao tác tiền ghi audit NGOÀI transaction → tiền commit nhưng log có thể mất → gộp create+mã+audit vào MỘT `$transaction` (widen `writeAudit` nhận client-trong-tx), áp cho createTransaction/writeOffBadDebt/createDebtReceipt/cancelCashEntry/createCashEntry → tiền+log ATOMIC. Regression: full suite 34/34 (money selftest #15/#25/#26/#27 assert giá trị chính xác vẫn xanh sau khi audit vào tx). Trước — R48 Pha 2 bảo mật đăng nhập: + B43 khóa tài khoản VĨNH VIỄN → DoS ai cũng khóa được mọi admin (sai 5 lần) phải sửa DB → **khóa TẠM 15 phút tự mở** (USER_AUTO_UNLOCKED); + B42 deviceInfo=hostname giả mạo được → qua mặt "đăng nhập thiết bị khác" → **device-GUID** bền ở userData làm khóa nhận diện; + B41 guard tin mỗi in-memory `current` → phiên bị đá/hết hạn VẪN còn quyền tới nhịp tim kế → `requirePermission` **re-validate phiên DB mỗi lần** (thu hồi ngay); + B40 forceChangePassword chỉ ở renderer → gọi IPC trực tiếp bypass được + re-auth cấp-2 không tính bộ đếm khóa → **chặn server-side + tính khóa**; + PF-05 block forceChangePassword làm vỡ nhiều selftest — **full-suite rerun sau đổi guard-core bắt** → fix createUser bỏ cờ trong selftest mode. Trước — R48 FIX đợt 2 — TIỀN BigInt + không cắt hiển thị: + B39 cột tiền là int4 (trần ~2,15 tỷ) trong khi validate cho tới 1e15 → GD/phiếu > 2,15 tỷ **CRASH tràn số** (unhandled). Mr.Long chốt "GD KHÔNG giới hạn giá trị" → đổi 7 cột tiền (transactions.amount+3 revenue, funds.opening_balance, cash_entries.amount, cash_debt_settlements.amount) sang **BigInt(int8)**; margin/phi ×1000 giữ Int. Boundary Number()/BigInt() + **BigInt.prototype.toJSON** (JSON.stringify(bigint) từng ném) — selftest revenue #15 thêm ca 50 tỷ lưu đúng, 76/76, full suite 34/34. + R49 chống CẮT hiển thị: 78 chỗ thêm whitespace-nowrap cho tiền/TID/MID/SĐT/mã/serial/MST/CCCD/số TK (tên/địa chỉ vẫn cho xuống dòng). Trước — R48 FIX đợt 1: + B38 lọc kỳ công nợ DebtPage chặn trên 23:59:59 thiếu ms → GD giây cuối bị bỏ (lớp B24) → .999; + B37 khách hàng ĐÃ XÓA MỀM vẫn gắn được GD mới → tái xuất hiện thành người nợ → chặn cả deletedAt; + B36 BACKUP hardening: (C1) backup tự động lỗi LẶNG không audit/không báo → AUTO_BACKUP_FAILED+notifyAdmins, (C2) thiếu watchdog "backup quá hạn" → backupWatchdog cảnh báo, (C3) không verify dump khi tạo → size floor + pg_restore --list, (C5) B20 chỉ cứu 1 bản → restore mất log B2/B3 → reconcile TOÀN BỘ; + B35 IPC file:read KHÔNG gắn quyền → mọi user đọc trộm ảnh CCCD/ĐKKD (IDOR) → gắn DOSSIER_VIEW/RCV_ACCT_VIEW + chặn traversal bằng resolve-trong-root; C7 báo kicked kèm tên thiết bị. selftest-backup #36 9/9. Còn nhiều finding CHỜ DUYỆT (BigInt tiền/realtime change-token/optimistic-lock/lockout policy/kiến trúc superuser). Trước đó — R46/R41 phiên đăng nhập + R47 font 13: + B34 selftest revenue #15 STALE — kỳ vọng đối soát công nợ thủ công `settleTransactions`→changed=1 nhưng tính năng đã TẮT từ Phase H2 (chuyển phiếu Thu công nợ) → DEBT_SETTLE_DISABLED; **full-suite rerun BẮT BUỘC sau đổi lõi auth phát hiện** (đúng rule [[feedback_audit_full_selftest_after_infra_change]]); R46 1-tài-khoản-1-thiết-bị + R41 danh sách online + R47 font export chuẩn 13. Trước đó cùng ngày — polish xuất Excel R43/R44/R45: + B33 chuỗi chứa chữ "đ" ("Đang hoạt động"/"đối tác") bị nhầm là TIỀN → căn phải sai, vì regex tiền dò `[đ]` bắt cả chữ cái Việt — sample nhân sự bắt trước ship; + B32 tên dài wrap 2 dòng bị CHE do khóa chiều cao hàng = 20 — Mr.Long production; export polish: R43 bỏ chấm vàng "số lưu dạng text" (ignoredErrors) · R44 căn cột theo kiểu (tiền phải/mã-SĐT-trạng thái giữa/tên dài trái) · R45 nút "Xuất mẫu (rỗng)" hiện ở thanh công cụ. Trước đó cùng ngày — R36/R38/R39 + deep-audit đối kháng R30/R34: + B31 export .xls-HTML giả gây Excel cảnh báo Yes/No khi mở + xấu — Mr.Long production; + B30 pg_advisory_xact_lock trả void, gọi qua $queryRaw → queryCompiler wasm không deserialize được → selftest 34 bắt TRƯỚC ship, đổi $executeRaw; + B29 approval_requests thiếu unique 1 yêu-cầu-CANCEL-PENDING/(loại,id) → race tạo 2 PENDING trùng — deep-audit; + B28 tid_sell_fees thiếu unique 1 override-active/(tid,thẻ) → race 2 dòng active → phí bán bất định = TIỀN SAI — deep-audit; + B27 LAST_ADMIN TOCTOU: 2 luồng duyệt xóa 2 Admin khác nhau đọc count admin cũ → có thể về 0 Admin (khóa hệ thống) — deep-audit; + PF-04 R36 nhật ký còn tiếng Anh: thêm AuditAction nhưng quên nhãn UI — Mr.Long production). Trước đó 2026-07-11 (+ B26 selftest R34 dùng username Manager 7 ký tự < tối thiểu 8 → createUser VALIDATION → login fail → cascade NOT_AUTHENTICATED giả; engine đúng, test sai fixture — LEAD bắt khi chạy throwaway; + B25 regression selftest R8b (countCustomers) viết SAI kỳ vọng `active +1` trong khi seed 2 khách ACTIVE → hàm đúng nhưng test FAIL — LEAD bắt khi rerun sạch, không phải lỗi sản phẩm; + B24 xếp hạng doanh số TID #13/#14: kỳ mặc định (bound local) vs kỳ chọn (dateRange UTC) lệch ~7h ICT → cùng tháng ra 2 số + GD sát biên phân loại nhầm — QA-pair đối kháng bắt trong review #13/#14; + B23 nhập Excel tạo ngay khi chọn file (không xem trước→xác nhận) → rủi ro nhân đôi phiếu thu/chi + xlsx@0.18.5 CVE + không cap dòng — QA-pair đối kháng bắt trong review #9; + B22 gán TID lên máy đã có TID khác → mồ côi TID + 2 binding mở (QA-pair đối kháng bắt trong review K2, ảnh hưởng cả assignTid K1 đã tag); … P1.2 approval + B17 emit-trap/clobber + B18 TOCTOU tương tranh approval — phát hiện khi chuẩn bị G10 đa-client Postgres; + PF-01 sót auto-update do duyệt gộp; + B19 admin reset MK user khác không re-auth actor; + B20 restore Postgres làm mất bản ghi backup an-toàn do pg_restore --clean ghi đè backup_logs; + B21 seed trạng thái nhập kho phân biệt hoa/thường đẻ bản gần trùng trên DB dữ liệu thật của Mr.Long — phát hiện khi nâng+seed DB `glb` chuẩn bị reopen 11/7).

### B77 — Health-Scan chạy lệnh SQLite `PRAGMA` trên PostgreSQL → nuốt lỗi im → BÁO OK GIẢ [FIXED] — audit ngoài 0.2.57
- **Phát hiện bởi:** audit độc lập trên installer 0.2.57 (Mr.Long chuyển lại) — LEAD **verify tại code thật** (`health-scan.ts:146,154`).
- **Nguyên nhân:** khi chuyển SQLite→PostgreSQL (G10), 2 check toàn vẹn `PRAGMA integrity_check` + `PRAGMA foreign_key_check` (cú pháp SQLite) GIỮ NGUYÊN. Trên PG chúng LUÔN throw lỗi cú pháp; `safe()` **chỉ `console.error` rồi nuốt** (không đẩy finding, không trừ đếm); `CHECKS_TOTAL=12` **hard-code** (thực tế 13 khối check — đã lệch sẵn 1). → Health-Scan báo "chạy đủ 12 check / OK" dù 2 kiểm tra toàn vẹn **KHÔNG hề chạy** suốt nhiều bản.
- **Fix:** (1) gỡ 2 check PRAGMA (vô nghĩa trên PG; toàn vẹn cấp-engine do PG lo, toàn vẹn nghiệp vụ đã có check orphan #3–#6). (2) `safe(name, fn)` — check nào ném lỗi PHẢI đẩy `CHECK_FAILED` (ERROR "không chạy được kiểm tra <name>") thay vì nuốt im. (3) `checksTotal` lấy **ĐỘNG** = `checksRun` (counter tăng trong safe) → không hard-code, không lệch được nữa (nay = 11).
- **Regression:** ST17 (HSC) — `checksTotal===11` (đếm động) + **`không có CHECK_FAILED trên DB lành`** (bẫy chính: PRAGMA/query lệch engine sẽ hiện CHECK_FAILED → test FAIL) + không còn DB_INTEGRITY/DB_FOREIGN_KEY. **Bug class (PF-10):** wrapper nuốt lỗi PHẢI biến lỗi thành finding + đếm động; đổi engine/adapter DB PHẢI test từng check chạy được — nối [[feedback_silent_swallow_false_pass_engine_migration]].

### B78 — Backup (pg_dump/pg_restore) chạy được ở MÁY TRẠM (không có PG tools) → fail khó hiểu [FIXED] — audit ngoài 0.2.57 (A1)
- **Phát hiện bởi:** audit độc lập — `backup-service.ts` tìm `GLB_PG_BIN → D:/PostgreSQL16/pgsql/bin → PATH`; máy trạm không cài PG client tools → backup/restore/backup-định-kỳ/pre-fix-backup fail.
- **Chốt (Mr.Long "A1"):** SAO LƯU CHỈ Ở MÁY CHỦ (không đóng gói pg_dump).
- **Fix:** gate `isServerRole()`: `createBackup` + `systemBackup` trả `BACKUP_SERVER_ONLY` (fail-closed rõ) trên máy trạm; scheduler bảo trì nền (`startHousekeeping` tick) `return` sớm nếu không phải server; UI `BackupPage` khóa nút "Tạo bản sao lưu ngay" → nhãn xám "Sao lưu chạy ở máy chủ" (đọc `serverConfigGet().serverRole`).
- **Regression:** ST36 (BACKUP) `A1: máy trạm createBackup → BACKUP_SERVER_ONLY` + `backup định kỳ ran=false`; **handtest playwright 2 ảnh** (máy chủ nút xanh / máy trạm khóa). **Bug class:** op phụ-thuộc-VAI-TRÒ-máy (thick-client→shared PG, model A) PHẢI gate role + fail-closed trên client.

### B79 — Cảnh báo dung lượng ổ đĩa đo NHẦM ổ khi PG ở máy chủ khác [FIXED] — audit ngoài 0.2.57 (#3)
- **Phát hiện bởi:** audit độc lập — `storage-service.ts` chạy `SHOW data_directory` (đường dẫn TRÊN MÁY CHỦ PG) rồi `statfsSync(dir)` trên máy đang chạy → máy trạm đo nhầm ổ mình (hoặc null); code cũ đã fail-safe null nhưng còn rủi ro hi hữu trùng path.
- **Fix:** `diskInfo()` gate `if (!isServerRole()) return null` — chỉ statfs khi máy đang chạy CHÍNH LÀ máy chủ PG; máy trạm trả disk*=null (thành thật "không đo được"), `dbBytes` (pg_database_size) vẫn đúng.
- **Regression:** ST16 (STORAGE) `#3: máy trạm → disk*=null + over=false` + `dbBytes>0 qua pg_database_size`. **Bug class:** như B78 — đo tài nguyên MÁY CHỦ phải chạy TRÊN máy chủ, không suy từ máy trạm.

### B80 — Icon app trên taskbar KHÔNG đồng bộ icon bản chuẩn [FIXED — chờ PV] — Mr.Long ("icon app mở ra ở taskbar không đồng bộ icon bản chuẩn")
- **Phát hiện bởi:** Mr.Long — mở app, icon taskbar không khớp icon shield chuẩn.
- **Nguyên nhân:** `createWindow` tạo `BrowserWindow` **không set `icon`** + app **không gọi `app.setAppUserModelId(appId)`**. Trên Windows, thiếu AppUserModelID → tiến trình đang chạy KHÔNG liên kết với shortcut đã cài (appId `com.globeway.glb`) → taskbar hiện icon mặc định/nhóm sai.
- **Fix:** (1) `app.setAppUserModelId('com.globeway.glb')` (khớp appId electron-builder) đặt SỚM trước tạo cửa sổ (win32). (2) `BrowserWindow` set `icon` = `build/icon.ico` (packaged: `process.resourcesPath/icon.ico` — thêm vào `extraResources`; dev: đọc trực tiếp). **CHỜ Production Validation:** icon taskbar chỉ nghiệm thu được khi CÀI bản mới + nhìn taskbar thật (không tự test bằng script). **Bug class:** app Electron Windows PHẢI setAppUserModelId khớp appId bộ cài để taskbar/pin dùng đúng icon + liên kết shortcut.

### B74 — Sinh bill số tiền LỚN (>500tr) LỖI không sinh được + engine không CÂN ĐỐI (1 dòng ôm hết / SP đắt phá tỉ lệ) [FIXED] — Mr.Long (sinh bill thật, "à là lỗi do số tiền hơn 500tr")
- **Phát hiện bởi:** Mr.Long trên production — sinh 1 hóa đơn số tiền thật >500tr → LỖI (engine không ghép nổi 1 HĐ ≤5 dòng cho số quá lớn) → vào `errors[]`.
- **Nguyên nhân:** (1) 1 hóa đơn tối đa 5 dòng; với thư viện SP giá lẻ, tổng ghép tối đa (sau khi thắt số lượng thực tế B72) không tới 500tr → target lớn luôn fail. (2) Engine cũ để 1 dòng ôm phần lớn tiền → mất cân đối; và SP mà **1 ĐƠN VỊ đã > 3× phần chia đều** (Tivi 8tr trong HĐ 11tr, phần chia 2.2tr → 8tr/2.2tr = 3.6×) vẫn được chọn → 1 dòng phá tỉ lệ cân đối (ratio 3.604).
- **Chốt thiết kế (Mr.Long):** hóa đơn bán lẻ THẬT không quá ~299tr; **KHÔNG tách hóa đơn** ("không được tách hóa đơn"); cân đối hợp lý ("trần thật hóa đơn thực tế cũng không quá 299tr nên cân đối hợp lý").
- **Fix:** viết lại `lineitem-gen` theo **giá trị**: mỗi dòng nhắm ~`target/M` tiền; `buildPools` lọc `minPrice ≤ price ≤ maxPrice` với `maxPrice = 3×globalShare` → **loại SP mà 1 đơn vị đã vượt 3× phần chia** khỏi pool dòng dẫn (nới bỏ minPrice trước, giữ maxPrice để ưu tiên cân đối); trần dòng theo `lineQtyCap(price, globalShare)`; dedup theo TÊN (đa dạng); `HARD_UNIT_CAP=200` backstop. Trần **299tr** enforce ở SERVICE (`REAL_CEILING`, >299tr → VALIDATION, không tách) + **CẢNH BÁO trên FORM** (Mr.Long "cảnh báo luôn số tiền sai để tránh lỗi"): dòng >299tr bôi ĐỎ + dòng cảnh báo + chặn nút "Sinh bill" (`overCeil` memo).
- **Regression:** vitest `CÂN ĐỐI: KHÔNG dòng nào ôm > 3.5× phần chia` (7 target tới 299tr × 15 lần) + `HĐ 299tr vẫn sinh được money-exact` + ST44 `B-realistic` (cân đối≤3.5, qty≤200, đa dạng, money-exact) + `service: >299tr → VALIDATION` + **TEST TAY** form 350tr đỏ/chặn nút. **Bug class:** engine sinh dữ liệu có **trần nghiệp vụ thực tế** (không chỉ trần kỹ thuật) phải enforce ở service VÀ cảnh báo tại form (fail-fast up-front), + ràng buộc CÂN ĐỐI phải loại phần tử mà 1 đơn vị đã phá ngưỡng — nối B72 (số lượng phi lý), B73 (guard target lớn).

### B75 — Engine treo main process ~30-50s với target lẻ khó ghép (spin hết retry) [FIXED] — agent-1 (review song song, HIGH)
- **Phát hiện bởi:** agent độc lập review song song (Mr.Long "gọi agent độc lập song song review toàn diện") — target lẻ nằm DƯỚI trần nhưng khó ghép chính xác → engine quay hết `retries × M × ~20 mức chiết khấu` ≈ 30-50 GIÂY, đơ giao diện (main process block, khác B73 vượt-trần-throw-ngay).
- **Fix:** ngân sách thời gian cứng `GEN_DEADLINE_MS=300` thread qua `generateLineItems → tryFindLines → tryFindLinesWithM` (kiểm `Date.now() > deadline` mỗi 128 attempt); + **hoist SORT** ra ngoài vòng retry (`buildPools` dựng pool + sort MỘT LẦN, trước đây sort mỗi attempt = O(retries·n·logn)). Quá hạn → null → throw (→ `errors[]`) như target bất khả thi.
- **Regression:** vitest `fast-throw over-cap <200ms` + ST44 `target lẻ khó ghép KHÔNG treo (<1.5s)`. **Bug class:** mọi vòng lặp tìm-kiếm-tổ-hợp chạy trong main process PHẢI có **deadline cứng** (không chỉ giới hạn số retry) — retry-count lớn vẫn treo nếu mỗi attempt rẻ; và bất biến bất-biến-theo-attempt (sort pool) phải hoist khỏi vòng lặp.

### B76 — Đồ cân (kg/lít) phá money-exact do trôi số thực (float) [FIXED] — agent-1 (review song song, MEDIUM)
- **Phát hiện bởi:** agent độc lập — dòng đơn vị thập phân (kg/lít, số lượng bước 0.1) tính `partial += price * qty` bằng float → `999_989.9999...` thay vì `999_990` → tổng Σ dòng lệch subtotal 1đ ngẫu nhiên → vi phạm bất biến money-exact.
- **Fix:** tích lũy `partial += Math.round(price * qty)` (đơn giá bội 10 → giá trị dòng NGUYÊN, `Math.round` khử sai số float); test sum cũng dùng `Math.round(l.price * l.qty)`.
- **Regression:** vitest `Σ Math.round(price*qty) === subtotal` với thư viện CÓ đơn vị thập phân + kiểm chiết khấu KHÔNG tautology (`Math.round(subtotal*(1-P/100)) ≈ target`). **Bug class:** cộng dồn tiền từ `giá × số-lượng-thập-phân` PHẢI làm tròn TỪNG dòng về số nguyên trước khi cộng — cấm để float trôi rồi so bằng.

### B71 — Seed thư viện GỐC renbill khớp ngành theo tên CASE-SENSITIVE → ĐẺ ngành trùng hoa/thường + ngành rỗng dư trên production [FIXED] — Mr.Long ("sao nhóm ngành trong bill giải trùng lặp?") · **F4 (0.2.56): mở rộng dedup sang NFD (dấu tổ hợp) + khoảng trắng thừa qua `normIndustryName` (NFC+gộp space+trim+lower); regression ST44 F4 đổi tên "  Siêu  thị  ".NFD → seed lại KHÔNG đẻ mới.**
- **Phát hiện bởi:** Mr.Long trên production (`glb` 192.168.1.6) — nhóm ngành trong Bill Giải Trình bị TRÙNG ("Vận tải" vs "Vận Tải", "Siêu thị" vs "Siêu Thị") + vài ngành RỖNG không SP.
- **Nguyên nhân (xác minh bằng query production):** `seedBillExplainLibrary` khớp ngành có sẵn bằng `db.industry.findFirst({ where: { name: indName } })` — **so khớp CHÍNH XÁC hoa/thường**. Production đã có "Vận Tải"(NGH01)/"Siêu Thị"(NGH02) (chữ T hoa, tạo tay từ trước); seed dùng tên "Vận tải"/"Siêu thị" (t thường) KHÔNG khớp → tạo bản MỚI trùng nghĩa (NGH09/NGH12) + tạo luôn ngành rỗng "Thu hộ"/"Gas" (seed cũ tạo cả ngành KHÔNG có SP).
- **Fix:** (1) seed chỉ tạo ngành **CÓ sản phẩm** (bỏ tạo ngành rỗng); (2) khớp **case-insensitive** `name: { equals: indName, mode: 'insensitive' }, deletedAt: null` → tái dùng ngành sẵn có bất kể hoa/thường, không đẻ trùng. (3) Production: backup (`glb_pre_industry_merge_20260716_103145.dump`) + merge SQL gộp trùng về 1 (giữ NGH có SP/TID), soft-delete bản dư → 8 ngành sạch.
- **Regression:** ST44 (D) — Siêu thị tồn tại + 199 SP + `chỉ 1 ngành Siêu thị (case-insensitive)`; **đổi tên ngành thành HOA "Siêu Thị" rồi seed lại → KHÔNG tạo ngành mới** (chứng minh khớp case-insensitive), vẫn 1 ngành. **Bug class (EXTEND):** mọi "tìm-hoặc-tạo master-data theo TÊN" phải chuẩn hóa (case-insensitive + trim + NFC) TRƯỚC khi so khớp, và chỉ tạo entity khi có dữ liệu con — nối B21 (seed trạng thái phân biệt hoa/thường đẻ bản gần trùng).

### B72 — Engine sinh dòng bill ra SỐ LƯỢNG PHI LÝ (40 nồi cơm điện / 100kg hành tây) để ép khớp tổng [FIXED] — Mr.Long ("ưu tiên sản phẩm hữu dụng số lượng phù hợp")
- **Phát hiện bởi:** Mr.Long — 1 hóa đơn mua 40 nồi cơm điện, hành tây 100kg là không đúng thực tế.
- **Nguyên nhân:** `tryFindLines` dùng trần số lượng cứng `[25,50,99]` (đồ cân ×10 → tới 990 đơn vị) → để khớp tổng tiền, engine sinh số lượng bất kỳ ≤ trần, không xét GIÁ hay ĐVT → SP đắt (nồi cơm 1.5tr) vẫn ra 40 cái, đồ cân (hành) ra cả tạ.
- **Fix:** `realisticMaxQty(unit, price, relax)` = min(trần-ĐVT [đồ cân 15 / đồ đếm 12], floor(SPEND_CAP 5tr / đơn giá)) × relax → **giá càng cao số lượng càng ít** (nồi cơm 1.5tr → tối đa 3 cái ở relax=1, 9 ở relax=3; không thể ra 40). Áp cho MỌI dòng (dẫn + cuối). 3 tier: relax=1 (thực tế, ưu tiên) → relax=1 fallback M → relax=3 (cuối, để không fail target hợp lệ; SPEND_CAP vẫn chặn SP đắt). Money-exact bất biến giữ nguyên.
- **Regression:** vitest thuần `lineitem-gen.test.ts` (`realisticMaxQty` + `KHÔNG dòng nào vượt trần relax≤3` + `nồi cơm ≤9` + `hành ≤45`) + ST44 (B-realistic) trên thư viện lẫn SP đắt/đồ cân/đồ rẻ, 40 lần sinh, tiền vẫn khớp CHÍNH XÁC. **Bug class:** sinh dữ liệu tự động PHẢI có **sanity/plausibility test** (số lượng/ĐVT/giá hợp lý), KHÔNG chỉ invariant toán học (money-exact) — test PASS về toán ≠ đúng nghiệp vụ.

### B73 — Target quá lớn cho 1 hóa đơn (5 dòng) làm engine SPIN hết retry → treo UI [FIXED]
- **Phát hiện bởi:** vitest A3 (test-found, khi thêm ca `500tr → throw`) — 1 hóa đơn tối đa 5 dòng × trần thực tế ≈ 46tr; target 500tr không thể ghép nhưng engine chạy 3 tier × ~20 mức chiết khấu × hàng nghìn retry ≈ vài phút mới chịu throw → đơ giao diện.
- **Fix:** guard nhanh `maxComposable(products)` = tổng 5 dòng GIÁ-TRỊ lớn nhất (đơn giá × trần relax=3); `tryFindLines` trả null NGAY nếu `target > maxComposable` → throw tức thì (degrade), service đẩy vào `errors[]`, không treo.
- **Regression:** vitest `target QUÁ LỚN → THROW` + ST44 `target 500tr → throw (degrade)`. **Bug class:** thuật toán tìm-kiếm-tổ-hợp phải có **chặn trên/dưới O(1)** trước vòng lặp retry — input phi lý không được biến thành treo.

### B1 (nợ kỹ thuật BILL-07) — File .xlsx mồ côi khi lưu DB thất bại SAU render [FIXED] — Mr.Long ("cấm nợ kỹ thuật")
- **Nguyên nhân:** `generateBills` render + ghi file .xlsx TRƯỚC khi `billExplain.create`. Nếu create ném (mất kết nối/constraint) → file rác nằm lại ổ đĩa, app báo lỗi nhưng file vẫn còn = trạng thái nửa vời.
- **Fix:** bọc `billExplain.create` trong try/catch — DB fail → `unlink(result.file)` (dọn file mồ côi) → trả `DB_FAILED`. Số HĐ đã cấp atomic (B70/BILL-06) nên dôi vài số vô hại.
- **Regression:** đã có nhánh xử lý; ST44 sinh-bill end-to-end vẫn xanh (đường thành công không đổi). **Bug class:** thao tác 2-pha (ghi file + ghi DB) — pha sau fail phải DỌN pha trước (không để rác), hoặc đảo thứ tự để rollback được.

### (Feature) Cột `priority` — ưu tiên SP hữu dụng khi sinh dòng bill (Mr.Long "sản phẩm nào hữu dụng ưu tiên")
- Thêm `Product.priority Int @default(0)` (migration `20260716120000_product_priority` + self-heal `ADD COLUMN IF NOT EXISTS`). Engine chọn SP có **trọng số (1+priority)** → SP ưu tiên cao xuất hiện áp đảo, SP thường (priority 0) vẫn có cơ hội; khi cả ngành priority=0 → random ĐỀU y như trước (không đổi hành vi). UI ProductForm + cột bảng + import/export mẫu (cột "Ưu tiên"). Regression: vitest `priority cao → chọn nhiều hơn hẳn` + ST44 (A') round-trip DB (lưu 50/clamp 1000/mặc định 0).

### B70 — Audit độc lập 0.2.53 (Codex/PING) Bill Giải Trình: 8 finding thật + 3 REJECT/CONDITIONAL [FIXED]
- **Phát hiện bởi:** audit 3-agent độc lập (`reports/PING_CLAUDE_LATEST_0.2.53_AUDIT_2026-07-16.md`). LEAD **phản biện + tái hiện từng mã bằng selftest** (không claim từ static). Tái dựng: `npm run selftest:bill` (ST44, failures=0). Verdict:
- **BILL-04 (Critical, REPRODUCED)** parse tiền `parseTargets` dùng `Number(String(raw).replace(/[^\d]/g,''))` → `-100`→100 (đổi dấu), `1.5`→15, `1e3`→13 = **biến đổi giá trị im lặng** (lớp B62). Fix: parse STRICT — number nguyên>0≤MAX_SAFE, string `^\d+$` (không strip); dị dạng → VALIDATION. Regression ST44 (B2) 7 ca `-100/1.5/1e3/1,000/abc/space/0`.
- **BILL-03 (High, REPRODUCED)** ngày `parseISODate` nhận `2026-02-31` (→`new Date` cuộn sang 3/3), rỗng→hôm nay (lớp B65). Fix: `parseStrictYmd` round-trip UTC → ngày không thật/dị dạng → VALIDATION; `new Date(Date.UTC(...))` không cuộn. Regression ST44 6 ca sai + 1 ca nhuận thật `2028-02-29` hợp lệ.
- **BILL-05 (High, REPRODUCED)** tổng/target ép Number → mất chữ số >2^53. Fix: target ≤ MAX_SAFE (engine dùng Number); **tổng = Σ BigInt**; FE gửi CHUỖI chữ số raw + `money()` nhận bigint. Regression ST44 `9007199254740993`→VALIDATION.
- **BILL-06 (High, REPRODUCED — concurrency thật)** số HĐ đọc-render-ghi NGOÀI transaction → 2 request đồng thời trùng số/dải. Fix: cấp DẢI số dưới `pg_advisory_xact_lock(561053)` trong 1 `$transaction`. Regression ST44 (B3) **Promise.all 2 generate → `after===before+6`** (dải KHÔNG chồng; nếu race sẽ +3). Bug class: **cấp-số/counter tài chính phải atomic dưới advisory lock, KHÔNG đọc-sửa-ghi tách rời** (nối B27/B58).
- **BILL-01 (High, REPRODUCED một phần)** ngành không kiểm tồn tại/xóa-mềm — ngành *không tồn tại* bị NO_PRODUCTS chặn gián tiếp, nhưng **ngành xóa mềm còn SP thì lọt**. Fix: kiểm `industry` tồn tại + `deletedAt=null`. Regression ST44 ngành-xóa-mềm + ngành-không-tồn-tại → VALIDATION.
- **BILL-02 (High → CONDITIONAL)** TID không ràng buộc HKD. Phản biện: TID là **tracking-only, KHÔNG in lên HĐ** (chốt Mr.Long) → tác hại thấp; nhưng dữ liệu chéo vô nghĩa → thêm guard NHẸ: chặn khi `tid.dossierId != null && ≠ dossierId` (cho phép TID chưa gắn HKD). Regression ST44 TID-thuộc-HKD-khác → VALIDATION.
- **BILL-09 (Medium/High, REPRODUCED)** IPC input dị dạng: `parseTargets` giả định iterable → `targets:{}`/`null` ném TypeError; `dossierId:'1'` lọt Prisma. Fix: `Array.isArray` guard + kiểm kiểu number cho dossierId/industryId/tidId → VALIDATION. Regression ST44 3 ca.
- **BILL-07 (Medium → PARTIAL/ACCEPTED)** file .xlsx mồ côi nếu DB fail sau render. Phản biện: file thừa **vô hại** (user xóa được); phần nguy hiểm (số HĐ) đã gộp fix BILL-06 (cấp dải atomic trước render — gap số vô hại). Không đổi thêm.
- **BILL-08 (High/Conditional → REJECTED)** outputDir/template path. Phản biện: người có quyền `PRODUCT_MANAGE` **tự chọn nơi lưu trên máy mình** = tính năng (giống chọn thư mục xuất Excel), UI còn chưa mở ô này; không nhận path từ web/không phải RCE. Không sửa (defense-in-depth optional).
- **FE53-05/06 (High, REPRODUCED)** `BillExplainPage` Promise.all/reload/import không catch + giá SP ép Number. Fix: try/catch/finally + `validPrice` VN-format strict (nhận "45.000"=45000, chặn `-100`/`1e3`, ≤MAX_SAFE). Regression ST44 import "45.000"→45000, "-100" loại, imported=3/skipped=4.
- **FE53-01/02/03 (code cũ ngoài feature, REPRODUCED)** `ImportModal` (busy kẹt khi parse/import reject), `ExportRequestPanel` (loading/dialog kẹt + reset đọc filter cũ qua `setTimeout(reload)`). Fix: try/catch/finally + bỏ `setTimeout(reload)` (dựa `useEffect([statusFilter])`).
- **Mismatch version + tái dựng ST44 (audit meta):** root `package.json` 0.1.0→**0.2.54** khớp desktop; thêm npm script `selftest`/`selftest:bill`/`selftest:core` để ST44 **tái dựng được** (`npm run selftest:bill`).
- **Regression toàn cục:** ST44 ~55 assert (CRUD+import+money-exact+degrade+seed+guard quyền + **B2 đối kháng 25 ca + B3 concurrency**) failures=0; vitest 272; typecheck web+node 0. **Quy trình (rút kinh nghiệm):** mọi service **tiền/ngày/counter** MỚI phải có ngay ca đối kháng malformed (âm/thập phân/mũ/chuỗi/>2^53/ngày-không-thật) + concurrency Promise.all TRƯỚC khi claim — không đợi audit ngoài bắt.

### B69 — Header bảng (thead dán) bị NÚT thao tác trong tbody ĐÈ khi cuộn [FIXED] — Mr.Long screenshot (UI render)
- **Phát hiện bởi:** Mr.Long (ảnh `GLB_2026-07-16T01-37-10.png`) — khi lăn chuột trong bảng, các nút ở cột "Thao tác" (và nội dung dòng) TRỒI LÊN ĐÈ hàng tiêu đề dán (sticky header) → chữ tiêu đề bị nút che, nhìn rối.
- **Nguyên nhân (đối kháng, xác minh tại DOM):** `<thead className="sticky top-0 bg-[#F8FAFC]">` có `position:sticky` nhưng **z-auto**; đồng thời nút icon trong `<tbody>` là phần tử **được định vị** (`className="relative"` / IconAction). Hai phần tử đều positioned + cùng z-auto → theo **thứ tự vẽ DOM**, tbody (sau) vẽ ĐÈ lên thead (trước). Không phải lỗi CSS riêng 1 bảng — mẫu `sticky top-0` dùng lặp khắp app.
- **Fix (bug-class, EXTEND không vá lẻ):** thêm `z-20` cho MỌI `thead.sticky top-0` → **39 thead / 28 file** (script literal-replace). Header luôn nổi trên nội dung cuộn. Áp cả bảng mới "Bill Giải Trình".
- **Regression:** TEST TAY playwright-electron — `getComputedStyle(thead.sticky).zIndex==='20'` + `position==='sticky'` (script `_ui-shot.mjs`, xác nhận `{found:true,zIndex:"20",position:"sticky"}`), screenshot cuộn bảng 199 SP Siêu thị thấy header nổi trên. Bug class: **phần tử `position:sticky` chồng lên phần tử `positioned` trong cùng ngăn xếp phải có z-index tường minh > 0 — z-auto để thứ tự DOM quyết định = header bị nội dung sau đè. Quy trình: đổi UI bảng dán PHẢI mở app cuộn thử + kiểm computed z-index (nối [[feedback_ui_must_screenshot_verify_before_ship]]).**

### B48 — Hủy phiếu THU tiền bán thiết bị (SALE_COLLECT) KHÔNG hoàn công nợ mua thiết bị [FIXED] — Tổng kiểm tra đối kháng (CRITICAL tiền)
- **Phát hiện bởi:** agent code-reviewer (logic), LEAD verify tại code (`cash-entry-service.ts:589-616` + `device-sale-service.ts:230-237/270-273`). **Kịch bản:** bán chịu máy 2tr → công nợ 2tr; thu 800k (tạo CashEntry `SALE_COLLECT` + `DeviceSaleSettlement`); vào Thu–Chi HỦY phiếu thu 800k đó → quỹ giảm 800k (đúng) NHƯNG công nợ mua thiết bị vẫn hiển thị đã trừ 800k (còn 1,2tr thay vì về 2tr) → báo cáo công nợ THIẾU số + thu lại bị chặn `ALREADY_SETTLED` khi remaining tưởng đã 0.
- **Nguyên nhân:** `cancelCashEntry` chỉ gỡ `cashDebtSettlement` (đối soát nợ POS per-transaction) mà KHÔNG gỡ `deviceSaleSettlement` (đối soát công nợ mua thiết bị). Cả phiếu thu-lúc-bán lẫn thu-thêm đều là `sourceType='SALE_COLLECT'` và tạo `deviceSaleSettlement.cashEntryId`; remaining của DeviceSale tính LIVE từ Σ settlement (không có cờ settled) nên settlement mồ côi = công nợ sai vĩnh viễn.
- **Fix:** trong `cancelCashEntry` (sau khi `moved.count===1` → chống hủy 2 lần), thêm `txc.deviceSaleSettlement.deleteMany({ where: { cashEntryId: id } })`. An toàn cho mọi loại phiếu (chỉ SALE_COLLECT mới có settlement trỏ về). KHÔNG cần join `CashEntry.status` ở các đường đọc tổng vì settlement bị XÓA vật lý khi hủy.
- **Regression:** `selftest-devicesale` #41 **Ca9** — bán chịu 2tr → thu 800k (quỹ +800k, còn nợ 1,2tr) → hủy phiếu SALE_COLLECT → quỹ về 0, **công nợ HOÀN về 2tr**, thu lại 2tr OK (không kẹt ALREADY_SETTLED). Bug class: **hủy chứng từ tiền PHẢI hoàn TẤT CẢ bản ghi đối soát phát sinh từ nó (mọi loại settlement), không chỉ 1 loại.**

### B49 — Xóa kho còn máy IN_STOCK → máy "mắc kẹt" [FIXED] — Tổng kiểm tra đối kháng
- **Phát hiện bởi:** agent code-reviewer, LEAD verify (`warehouse-service.ts:213-227`). **Kịch bản:** kho K có máy đang tồn (IN_STOCK, `warehouseId=K`); xóa mềm kho K → máy vẫn trỏ `warehouseId=K` (kho đã xóa) → cột Kho rỗng, giao máy đọc kho báo NOT_FOUND → máy không thao tác được.
- **Nguyên nhân:** `deleteWarehouses` xóa mềm không kiểm con trỏ tới. Model-1 bất biến `warehouseId≠null ⟺ IN_STOCK` nên mọi máy trỏ vào kho đều IN_STOCK.
- **Fix:** trước khi xóa, đếm `posDevice.count({ warehouseId: id, deletedAt: null })`; >0 → `IN_USE` (chặn CẢ lô, thông báo rõ mã kho + số máy) + audit denied. Chỉ xóa khi mọi kho trong lô đều rỗng.
- **Regression:** #41 **Ca10** — máy vào kho DSKDEL → xóa kho → `IN_USE` (kho vẫn còn, máy giao lại được) → giao máy đi (rời kho) → xóa kho OK. Bug class: **xóa master-data PHẢI chặn khi còn bản ghi con trỏ tới (referential guard ở tầng nghiệp vụ).**

### B50 — user:create bỏ qua session guard DB → bypass forceChangePassword/TTL/khóa [FIXED] — Tổng kiểm tra đối kháng (security)
- **Phát hiện bởi:** agent security-auditor R48 (MEDIUM), LEAD verify (`user-service.ts:159-172`). **Kịch bản:** admin đang bị cờ `forceChangePassword` (mật khẩu tạm) gọi thẳng IPC `user:create` từ renderer → tạo tài khoản mới TRƯỚC khi đổi mật khẩu; hoặc phiên đã hết TTL / bị đá / user bị LOCKED vẫn tạo được tới nhịp tim kế.
- **Nguyên nhân:** `createUser` dùng `me()` in-memory + `hasPermission` trực tiếp, KHÔNG qua `requirePermission`/`validateCurrentSession` (re-đọc phiên DB + TTL + trạng thái user + cờ forceChangePassword) như mọi handler khác.
- **Fix:** đầu `createUser` gọi `validateCurrentSession()` → NOT_AUTHENTICATED nếu phiên chết; `forceChangePassword` → MUST_CHANGE_PASSWORD; rồi mới nhánh 2-quyền (USER_CREATE / USER_CREATE_LIMITED) + scope cũ. Gỡ import `me` thừa.
- **Regression:** guard-core (đã có `selftest-session` #35 + `selftest-guard` #21 kiểm session-revalidate cho các handler qua requirePermission). Bug class: **MỌI handler đổi trạng thái PHẢI qua 1 cổng session-revalidate DB thống nhất — không tự dùng snapshot in-memory.**

### B51 — file:open nhận đường dẫn tùy ý → RCE qua XSS [FIXED] · B52 — file:read dùng mã quyền KHÔNG tồn tại → fail-closed âm thầm [FIXED] — Tổng kiểm tra đối kháng (security)
- **B51 phát hiện bởi:** security-auditor R48 (MEDIUM), LEAD verify (`ipc.ts:368-371`). **Nguyên nhân:** `ipcMain.handle('file:open', (_e,path) => shell.openPath(path))` truyền path tùy ý từ renderer → `shell.openPath` dùng ShellExecute → path `.exe/.bat/.cmd/.lnk/\\UNC` bị THỰC THI. Kênh chỉ để mở .xlsx vừa xuất, nhưng XSS qua trường free-text (tên KH/HKD/ghi chú) có thể ép mở payload. **Fix:** ghi mọi path `report:export` xuất trong phiên vào `exportedFiles` set; `file:open` chỉ mở khi path ∈ set (ngược lại "Chỉ mở được file vừa xuất từ phần mềm").
- **B52 phát hiện bởi:** security-auditor R48 (LOW nhưng là bug CHỨC NĂNG), LEAD verify (`ipc.ts:289-296` + `permissions.ts:51/54`). **Nguyên nhân:** `file:read` gác bằng mã `'DOSSIER_VIEW'`/`'RCV_ACCT_VIEW'` — KHÔNG tồn tại trong PERMISSION_CODES (đúng là `CONFIG_DOSSIER_VIEW`/`CONFIG_RCV_ACCT_VIEW`) → `requirePermission` từ chối MỌI người kể cả ADMIN (fail-CLOSED) → xem ảnh CCCD/ĐKKD chết âm thầm. An toàn bảo mật (fail-closed) nhưng mất tính năng không ai biết. **Fix:** đúng mã `CONFIG_*`; thêm `setWindowOpenHandler` chỉ `shell.openExternal` khi scheme http/https (chặn scheme lạ).
- **Regression:** **vitest guard `ipc-permcode.test.ts`** — quét TĨNH toàn bộ `main/*.ts`, trích mọi mã quyền `requirePermission('CODE'` + biến `perm` (ternary chọn quyền), assert TẤT CẢ ∈ `PERMISSION_CODES`. Bắt được lớp lỗi mà selftest KHÔNG bắt (selftest gọi thẳng service, không qua IPC, không so mã với danh mục). Bug class: **mã quyền dùng để gác cổng PHẢI được kiểm tồn tại tự động (mã gõ sai = fail-closed mất tính năng âm thầm); path từ renderer KHÔNG được đưa thẳng vào shell.**

### B46 — Sửa đè im lặng (lost update) trên MỌI form Sửa [FIXED] — R48 Pha 3b optimistic-lock
- **Phát hiện bởi:** LEAD (R48 #2, Mr.Long yêu cầu "chống 2 người sửa đè"). **Kịch bản:** A và B cùng mở khách hàng KH03; A sửa SĐT + Lưu; B (vẫn thấy dữ liệu cũ) sửa địa chỉ + Lưu SAU → ghi đè, thay đổi SĐT của A biến mất, không cảnh báo.
- **Nguyên nhân:** update-service ghi đè theo `id` không kiểm bản ghi đã đổi kể từ lúc client tải.
- **Fix:** helper `optimistic-lock.ts::staleGuard(current, expected)` — chèn ngay sau `findUnique` trong 21 update fn (customer/bank/cardType/partner/dossierSource/dossier/tidStatus/tidConfig/fund/role/feeType/user/supplier/posModel/intakeStatus/posIntake/statusOption/rcvSource/rcvAccount/industry/cashCategory). Client 20 form gửi `expectedUpdatedAt = row.updatedAt`, xử lý `STALE_WRITE` (toast + reload). 20 DTO lộ `updatedAt` (17 qua AuditTrail mixin, 3 phẳng Role/User/StatusOption). **Tương thích ngược:** thiếu mốc → không kiểm → selftest cũ + call nội bộ không vỡ.
- **Regression:** `selftest-optlock` #37 (customer: mốc cũ→STALE_WRITE + KHÔNG đè, mốc mới→ok, không mốc→ok, mốc rác→bỏ qua; industry+fund kiểm 2 kiểu DTO lộ updatedAt). Loại khỏi phạm vi (có lý do): bill (bất biến — sửa qua hủy+tạo lại), settings/storage (singleton config, không phải bản ghi 2-người-sửa). Bug class: **mọi update entity qua form PHẢI qua optimistic-lock; mọi DTO sửa-được PHẢI lộ updatedAt để client echo.**

### B44 — Audit ghi NGOÀI transaction tiền → crash mất log / GD thiếu mã [FIXED] · B45 — Phí bán/mua < phí cài máy → doanh thu ÂM [FIXED] — R48 Pha 3a
- **B44 phát hiện bởi:** LEAD rà lại luồng tiền (R48 toàn vẹn). **Nguyên nhân:** `createTransaction` tạo GD rồi mới `update` gán mã `GD<id>` và `writeAudit` ở các lệnh riêng, NGOÀI `$transaction` → nếu tiến trình chết giữa chừng: GD tồn tại nhưng chưa có mã / tiền đã commit nhưng dòng audit không ghi được (mất vết). **Fix:** widen `writeAudit(db: Pick<Db,'auditLog'>)` để nhận client trong `$transaction`; bọc create+gán-mã+audit vào MỘT `$transaction` cho `createTransaction`; chuyển `writeAudit` VÀO trong `$transaction` sẵn có của `writeOffBadDebt`, `createDebtReceipt`, `cancelCashEntry`, `createCashEntry` → tiền và log commit/rollback CÙNG nhau.
- **B45 phát hiện bởi:** LEAD (R48). **Nguyên nhân:** `setFeeRate` (biểu phí niêm yết) và `setTidSellFees` (phí bán thực tế theo TID) không kiểm phí bán/mua so với phí cài máy → set phí bán < phí cài máy → chênh khách (CL_KH = bán − cài máy) ÂM → doanh thu/công nợ âm ngầm, khó truy. **Fix:** `setFeeRate` chặn `phiMua<phiCaiMay` và `phiBan<phiCaiMay` (VALIDATION); `setTidSellFees` chặn phí bán thực tế < phí cài máy hiệu lực tại ngày (pickEffectiveRate). Bug class: **mọi cấu hình phí phải đảm bảo chênh ≥ 0 trước khi lưu — không để giá trị âm phát sinh khi tính GD.**
- **Regression:** full suite 34/34 sau khi audit chuyển vào transaction (money selftests #15 76/76, #25/#26/#27 assert giá trị chính xác — chứng minh audit-in-tx không phá atomic tiền, không nhân/mất dòng). Bug class B44: **audit của thao tác tiền PHẢI ở cùng $transaction với thao tác đó.**

### B36 — BACKUP hardening (4 lỗ "sót/sai backup") [FIXED] — R48 agent backup
- **Phát hiện bởi:** agent code-reviewer đối kháng (R48), LEAD verify tại code.
- **Nguyên nhân + Fix:**
  - **C1** `systemBackupIfDue`/`systemWeeklyMaintenanceIfDue` khi backup lỗi → `return {ran:false}` LẶNG (không audit, không báo) → dữ liệu không được sao lưu mà không ai biết. Fix: `reportBackupFailure` ghi audit `AUTO_BACKUP_FAILED` + `notifyAdmins` (throttle 6h) + lưu `lastFailureAt`.
  - **C2** không có watchdog "backup quá cũ". Fix: `backupWatchdog` (gọi mỗi giờ trong housekeeping) — chưa từng backup hoặc >2× chu kỳ → `BACKUP_STALE` + notifyAdmins (throttle 24h).
  - **C3** không verify dump khi TẠO → dump 0-byte/cụt vẫn ghi nhận thành công. Fix: sàn kích thước 512B + `pg_restore --list` phải parse được TOC, nếu không → TỪ CHỐI.
  - **C5** B20 chỉ re-insert 1 dòng pre-restore → restore từ bản CŨ làm mất log các bản MỚI hơn (B2,B3). Fix: chụp TOÀN BỘ `backup_logs` trước restore, reconcile re-insert mọi dòng thiếu theo filePath.
- **Regression:** `selftest-backup` #36 (9/9): 3 backup + restore B1 → B2/B3 log còn · watchdog stale ghi `BACKUP_STALE` · pg_dump lỗi ghi `AUTO_BACKUP_FAILED`. Bug class: **backup KHÔNG được thất bại lặng / KHÔNG ghi nhận dump chưa verify**.

### B35 — IPC `file:read` không gắn quyền → IDOR đọc trộm ảnh CCCD/ĐKKD [FIXED] — R48 agent bảo mật
- **Phát hiện bởi:** agent security-auditor (R48). PoC: từ renderer lặp `readAttachment('dossier/<id>/...')` không cần quyền hồ sơ.
- **Nguyên nhân:** `ipc file:read` gọi thẳng `readAttachmentDataUrl` KHÔNG `requirePermission`; chỉ chặn traversal bằng `includes('..')` (yếu).
- **Fix:** gắn quyền theo tiền tố (dossier→`DOSSIER_VIEW`, receiveAccount→`RCV_ACCT_VIEW`, lạ→chặn); `readAttachmentDataUrl` chặn traversal bằng `resolve()` + bắt buộc nằm TRONG uploadsRoot (không dựa chuỗi '..'). Bug class: **mọi IPC trả dữ liệu nhạy cảm PHẢI qua requirePermission; path người dùng phải containment bằng resolve-trong-root.**

### B37 — Khách hàng đã XÓA MỀM vẫn gắn được giao dịch mới [FIXED] · B38 — lọc kỳ DebtPage thiếu ms (lớp B24) [FIXED] — R48 agent dữ liệu
- **B37:** guard chặn khách LOCKED/CANCELLED chỉ chạy khi `deletedAt==null` → khách đã hủy qua duyệt vẫn tạo được GD → tái xuất hiện thành người nợ. Fix: `!cust || deletedAt != null` → CHẶN. (regression: selftest revenue tạo GD khách sống vẫn xanh.)
- **B38:** `DebtPage` `new Date(fTo+'T23:59:59')` thiếu `.999` → GD `23:59:59.xxx` bị rớt khỏi `lte` (đúng lớp B24, đã đúng ở cash-entry). Fix: `.999`.

### B34 — Selftest revenue #15 STALE: kỳ vọng đối soát công nợ thủ công (đã bị tắt từ Phase H2) [FIXED]
- **Phát hiện bởi:** **full-suite rerun BẮT BUỘC sau khi đổi lõi auth (R46)** — 3 assertion fail ở REV15 (`settleTransactions→changed=1`, công nợ còn 140.000, lọc chưa-đối-soát chỉ 1 GD). KHÔNG phải hồi quy do R46 (login pattern selftest sạch: logout trước khi đổi user); là test cũ chưa cập nhật.
- **Nguyên nhân:** đối soát công nợ THỦ CÔNG (`settleTransactions(..., true)`) đã bị **cố ý TẮT** khi chuyển sang phiếu Thu công nợ (Phase H2) → trả `DEBT_SETTLE_DISABLED`; nhưng selftest-revenue vẫn kỳ vọng hành vi CŨ (changed=1 + công nợ giảm). Test lỗi, sản phẩm đúng.
- **Fix:** sửa 3 assertion phản ánh hành vi HIỆN TẠI — settle thủ công → `DEBT_SETTLE_DISABLED`, công nợ KHÔNG đổi (490.000), chưa-đối-soát vẫn 2 GD. REV15 73/73.
- **Đề xuất quy trình:** khi TẮT/đổi 1 tính năng, PHẢI rà selftest đang test tính năng đó ngay (không để stale tới lần rerun sau). Củng cố [[feedback_audit_full_selftest_after_infra_change]] — đổi lõi (auth/DB) → rerun TOÀN BỘ; chính lần này bắt được test mồ côi.

### B33 — Chuỗi chứa chữ cái "đ" bị nhầm là TIỀN → căn phải sai [FIXED]
- **Phát hiện bởi:** LEAD khi sinh sample "Danh sách nhân sự" để duyệt R44 — TRƯỚC ship. Cột "Trạng thái" ("Đang hoạt động"/"Đã khóa") bị căn PHẢI như tiền.
- **Nguyên nhân:** `isMoneyString` dùng `/[₫đ]|VND/i` — nhưng "đ" là **chữ cái tiếng Việt cực phổ biến** (Đang, động, đối, đã…) → mọi chuỗi chứa "đ" bị coi là tiền → cột toàn text vẫn `allRightable=true` → căn phải.
- **Fix:** `isMoneyString` yêu cầu **TOÀN BỘ** chuỗi là số/phân tách + ký hiệu tiền CHỈ ở CUỐI (`^[\d.,\s]+(\s?(₫|đ|VND))?$`) rồi mới xét có phân tách nghìn / ký hiệu tiền. "Đang hoạt động" (có chữ) → loại; "1.000.000đ" → nhận.
- **Regression:** vitest `excel-export` — "Đang hoạt động" + "đối tác lâu năm" → căn GIỮA (không phải phải). Bug class: **regex nhận diện tiền/số CẤM dùng ký tự trùng chữ cái tiếng Việt ("đ") không neo vị trí — phải khớp toàn chuỗi.**

### B32 — Tên dài wrap 2 dòng bị CHE chữ do khóa chiều cao hàng = 20 [FIXED]
- **Phát hiện bởi:** Mr.Long khi mở file xuất (production) — "Tên ngân hàng bị tràn 2 dòng bị che chữ".
- **Nguyên nhân:** ô văn bản bật `wrapText` nhưng `row.height` bị đặt CỨNG = 20 → tên >30 ký tự wrap 2 dòng bị cắt dòng 2.
- **Fix:** `rowHeightFor()` tính chiều cao theo SỐ DÒNG WRAP nhiều nhất trong hàng (ước theo độ rộng cột × độ dài ô, 15pt/dòng) — áp cho cả hàng tiêu đề lẫn dữ liệu; chỉ cột căn trái (tên/địa chỉ) mới wrap nên chỉ tính theo cột đó.
- **Regression:** vitest — tên 44 ký tự (cột ~30) → hàng cao hơn base 20 và cao hơn hàng tên ngắn. Bug class: **ô wrapText CẤM đặt chiều cao hàng cứng — phải tính đủ số dòng wrap.**

### B31 — Xuất Excel ".xls HTML-table giả" → Excel cảnh báo Yes/No khi mở + xấu (không IN HOA/lọc/A4) [FIXED]
- **Phát hiện bởi:** Mr.Long khi mở file xuất thực tế (production) — "file excel xuất ra quá xấu", "lỗi lúc mở file yêu cầu yes/no", "sao cỡ chữ 10", đối chiếu mẫu biểu các tool GLOBEWAY sẵn có.
- **Nguyên nhân:** `exportCsv` cũ ghi chuỗi HTML `<table>` với đuôi `.xls` (MIME `application/vnd.ms-excel`). Excel mở được nhưng vì **định dạng thật ≠ đuôi** nên bật hộp cảnh báo Yes/No mỗi lần mở; đồng thời không có page-setup A4, không IN HOA tiêu đề, không autofilter, tải kiểu trình duyệt (rơi Downloads, không hộp thoại chọn nơi lưu / không hỏi mở).
- **Fix (R38/R39):** thay bằng **.xlsx THẬT** dựng ở main qua `exceljs` (`export-service.ts` — tông nhà GLOBEWAY: tiêu đề `#2E75B6` chữ trắng, tổng hợp vàng kem, tên cột IN HOA nền xanh nhạt + autofilter + đóng băng, kẻ ô mảnh, dòng lẻ xám nhạt, Times New Roman 11pt, A4 DỌC fit-1-trang). IPC `report:export` → `dialog.showSaveDialog` (tên gợi ý "Danh sách … dd.m.yyyy.xlsx", nhớ thư mục cũ) → ghi file → hộp thoại DÙNG CHUNG "Mở / Không mở" (`shell.openPath`). `exportCsv` giữ nguyên chữ ký (28 nơi gọi không đổi) nhưng bên trong đi IPC + map khóa→tên Việt. Mẫu nhập + báo cáo dòng lỗi cùng luồng.
- **Regression:** vitest `excel-export.test.ts` (8 ca) trên `buildReportWorkbook`/`buildTemplateWorkbook`: IN HOA tiêu đề+cột · A4 portrait fitToWidth · autofilter + freeze ySplit=3 · số căn phải/chữ căn trái · sheet "Mẫu nhập" GIỮ nhãn gốc dòng 1 (nhập lại khớp). Bug class: **cấm xuất pseudo-.xls (HTML-table đuôi .xls)** — mọi export dùng builder .xlsx thật.

### B30 — `pg_advisory_xact_lock` (kiểu void) gọi qua `$queryRaw` → queryCompiler wasm không deserialize được [FIXED]
- **Phát hiện bởi:** selftest 34 (entity-cancel) khi kiểm chứng bản vá B27 — **TRƯỚC khi ship** (đúng rule gate). `approveEntityCancel(User)` ném `PrismaClientKnownRequestError: Failed to deserialize column of type 'void'`.
- **Nguyên nhân:** dùng `txc.$queryRawUnsafe('SELECT pg_advisory_xact_lock(...)')`; hàm trả SQL `void`, `$queryRaw` cố deserialize cột → Prisma 7 queryCompiler (wasm) lỗi. (`writeOffBadDebt` không dính vì `SELECT id … FOR UPDATE` trả cột int.)
- **Fix:** đổi sang `$executeRawUnsafe` (chỉ trả số dòng, không deserialize cột) — vẫn giữ khóa advisory trong transaction.
- **Regression:** chính selftest 34 (27/27) chạy qua đường duyệt-xóa User = đi qua advisory lock; xanh = không còn void-deserialize. Bug class: **khóa/side-effect SQL trả void PHẢI gọi qua `$executeRaw`, không `$queryRaw`.**

### B29 — `approval_requests` thiếu ràng buộc "1 yêu cầu CANCEL PENDING / (loại,id)" → race tạo 2 PENDING trùng [FIXED]
- **Phát hiện bởi:** deep-audit đối kháng (security-auditor độc lập) sau khi build R34 — có PoC, Postgres READ COMMITTED.
- **Nguyên nhân:** chống trùng chỉ bằng `findFirst(PENDING)` + tạo trong `$transaction`; không có unique index → 2 luồng `entityCancel:request` đồng thời cùng (loại,id) đều qua findFirst (chưa thấy insert của nhau) → 2 PENDING. (Tác hại giới hạn: duyệt cái thứ 2 → INVALID_STATE, nhưng còn 1 PENDING mồ côi + rối người dùng.)
- **Fix:** migration `20260712100000_r30r34_concurrency_guards` thêm **partial-unique** `approval_requests_pending_cancel_uq (entity_type, entity_id) WHERE action='CANCEL' AND status='PENDING'` (dùng chung cho cả bill-cancel) + bắt P2002 ở `requestEntityCancel` → ALREADY_PENDING.
- **Regression:** selftest 34 +1 — khẳng định index tồn tại (pg_indexes). Bug class: xem B28.

### B28 — `tid_sell_fees` thiếu ràng buộc "1 override active / (tid,thẻ)" → race 2 dòng active → phí bán bất định = TIỀN SAI [FIXED]
- **Phát hiện bởi:** deep-audit đối kháng (security-auditor độc lập) — mức MEDIUM (tiền), có PoC.
- **Nguyên nhân:** upsert bằng `findFirst`+`create` không unique (bỏ @@unique vì soft-delete — B05). 2 lệnh `tid:sellFeeSet` đồng thời cùng (tid,thẻ) → 2 dòng active phí khác nhau; `resolveFeeForTxn` `findFirst` KHÔNG orderBy → chọn dòng bất định → `sellMarginMilli` (chốt vào bill bất biến) sai/khác nhau cùng input.
- **Fix:** (a) migration thêm **partial-unique** `tid_sell_fees_active_uq (tid_id, card_type_id) WHERE deleted_at IS NULL` (tôn trọng soft-delete); (b) `setTidSellFees` khóa `SELECT id FROM tids … FOR UPDATE` đầu transaction → serialize 2 lệnh cùng TID; (c) `resolveFeeForTxn` + `listTidSellFees` thêm `orderBy id desc/asc` → xác định (chọn override mới nhất) kể cả nếu lỡ có >1.
- **Regression:** selftest 33 +3 — index tồn tại · chèn thẳng dòng active trùng bị chặn (P2002) · chỉ 1 override active/(tid,thẻ). Bug class: **bảng có soft-delete cần "1 active / khóa nghiệp vụ" PHẢI dùng partial-unique `WHERE deleted_at IS NULL` (không bỏ trần chỉ vì soft-delete) + đọc có orderBy xác định.**

### B27 — LAST_ADMIN TOCTOU: 2 luồng duyệt xóa 2 Admin khác nhau → có thể về 0 Admin (khóa toàn hệ thống) [FIXED]
- **Phát hiện bởi:** deep-audit đối kháng (security-auditor độc lập) — MEDIUM (likelihood thấp, tác hại cao), có PoC.
- **Nguyên nhân:** guard "không xóa Admin cuối" đọc `countOtherActiveAdmins` bằng `findMany` thường trong transaction, KHÔNG khóa hàng (khác `writeOffBadDebt` có `FOR UPDATE`). READ COMMITTED: 2 duyệt xóa 2 Admin khác nhau đọc count cũ (chưa thấy commit của nhau) → cùng qua guard → 0 Admin.
- **Fix:** `applyCancel(User)` lấy **advisory xact lock chung** `pg_advisory_xact_lock(748301)` (qua `$executeRaw` — xem B30) trước khi đếm → serialize mọi luồng duyệt-xóa User; luồng sau đọc count SAU khi luồng trước commit → guard giữ.
- **Regression:** selftest 34 (27/27) đi qua đường duyệt-xóa User. Bug class: **bất biến "còn ≥1 bản ghi thiết yếu" (Admin cuối…) kiểm trong transaction PHẢI serialize bằng khóa (advisory/FOR UPDATE), không đọc count trần.** Nối [[feedback_audit_full_selftest_after_infra_change]].

### B25 (PF-02) — Regression selftest R8b `countCustomers` viết SAI kỳ vọng (`active +1` trong khi seed 2 khách ACTIVE) [FIXED]
- **Phát hiện bởi:** LEAD khi rerun selftest 3 từ trạng thái sạch (đúng rule "AUDIT không tin số agent, rerun sạch"). Agent CMD_BUILD (R8) tự thêm khối regression `countCustomers` nhưng ghi assert `a.active === b.active + 1`, trong khi seed tạo **2 khách ACTIVE** (CC Active có đại lý + CC Unassigned không truyền status → mặc định ACTIVE) → thực tế +2. Hàm `countCustomers` TRẢ ĐÚNG (+2); chính test sai kỳ vọng → FAIL giả.
- **Nguyên nhân:** người viết test suy luận "4 khách = mỗi trạng thái 1" nhưng khách "Unassigned" mặc định ACTIVE nên có 2 ACTIVE. Lỗ: kỳ vọng test không khớp dữ liệu seed thực.
- **Fix:** sửa assert thành `a.active === b.active + 2` (kèm chú thích "CC Active + CC Unassigned mặc định ACTIVE"). selftest 3 failures=0.
- **Regression / Đề xuất quy trình:** (1) **AUDIT/LEAD PHẢI rerun mọi regression do agent thêm từ DB sạch trước khi tin** — không nhận "PASS" trong báo cáo agent (agent này báo PASS nhưng chưa chạy thật trên throwaway). (2) Khi seed test có field mặc định (status/agent…), liệt kê rõ giá trị mặc định trong comment để kỳ vọng khớp. Nối [[feedback_multiagent_audit_process_failures_10_7]] (đo/kiểm bằng rerun sạch, không tin số agent).

### B24 — Xếp hạng doanh số TID (#13) + Kỳ giao (#14): bất nhất múi giờ giữa 2 đường tính "kỳ" → cùng tháng ra 2 con số + GD sát biên phân loại nhầm [FIXED]
- **Phát hiện bởi:** QA-pair đối kháng (code-reviewer độc lập) trong review #13/#14, TRƯỚC khi LEAD commit — không lọt tới người dùng. Lõi tài chính (Σ doanh số, loại CANCELLED/writtenOff, quyền REVENUE_VIEW không rò rỉ) đã đúng; lỗ nằm ở quy ước biên kỳ.
- **Nguyên nhân:** `tidRevenueRanking` có 2 đường tính bound kỳ KHÔNG cùng quy ước: (a) mặc định (from/to trống) dùng `new Date(y, m, 1)` = **nửa đêm LOCAL**, half-open `[gte, lt)` — đúng chuẩn app (dashboard `computeMonthProfit`); (b) có from/to (kể cả khi bấm month-picker → gửi `yyyy-mm-01..lastDay`) đi qua `dateRange()` = `new Date('yyyy-mm-dd')` **nửa đêm UTC** + `lte` end-of-day-UTC. Trên máy ICT (UTC+7) hai biên lệch ~7h → user thấy "Tháng 7" (bound local) rồi bấm lại đúng Tháng 7 trên picker → **tổng đổi số**; GD lúc `01/07 03:00 ICT` bị loại, GD `01/08 05:00 ICT` bị kéo nhầm vào Tháng 7. `txnDate` là `DateTime` thường (không Timestamptz) càng dễ lệch. Cùng lớp lỗi ở #14 Kỳ giao (`deliveredAt` cũng lọc qua `dateRange` UTC → TID giao rạng sáng đầu kỳ bị loại nhầm). Lỗ TEST: mọi GD/deliver trong selftest seed vào GIỮA TRƯA (`cur(...,12:00)`) nên biên hoàn toàn hở.
- **Fix:** thêm `localDayBounds(from,to)` (tid-service) — parse `yyyy-mm-dd` bằng components → `new Date(y,m-1,d)` LOCAL, `to` inclusive → nửa đêm ngày kế (half-open `lt`), ĐỒNG NHẤT đường mặc định + dashboard. `tidRevenueRanking` đường có-filter và `listTids` (#14 Kỳ giao) đều chuyển sang `localDayBounds` thay `dateRange`. Một endpoint tiền tệ chỉ có MỘT quy ước biên. (`dateRange` giữ nguyên cho các filter cũ ngoài scope — openedAt/customer/transaction list; không đụng để tránh hồi quy.) + phụ (S-1): `db.tid.findMany` trong ranking thêm `deletedAt:null` + `filter(tidMap.has)` re-rank → TID soft-deleted không hiện bảng xếp hạng (đồng nhất listTids).
- **Regression:** selftest 30 (tidunify) — **B24 biên tháng**: seed GD `00:30 mùng 1` (local, trong kỳ) + `05:00 mùng 1 tháng sau` (ngoài) cho TID-K2-R4B → assert kỳ **mặc định == kỳ chọn-tháng-này == 4,000,000** (trước fix đường explicit ICT ra 0/khác); + R3 loại đủ **CANCELLED + CANCEL_PENDING + writtenOff + deleted** (chỉ 1M); + positive **ACCOUNTANT (có REVENUE_VIEW) → ranking OK** (khoá grant role cũ). failures=0.
- **Đề xuất quy trình (bug class "endpoint tiền tệ/kỳ có ≥2 đường tính bound không cùng quy ước múi giờ"):** MỌI endpoint tổng hợp theo kỳ PHẢI dùng CHUNG một hàm bound (local half-open), KHÔNG trộn `dateRange` UTC với bound local. Selftest kỳ/biên BẮT BUỘC seed dữ liệu **sát biên ngày/tháng** (rạng sáng mùng 1, đêm cuối tháng), không chỉ giữa trưa — nếu không lớp bug múi giờ vô hình. Nối [[feedback_ui_test_gap_paired_qa_loop_10_7]].

### B23 — Nhập Excel tạo bản ghi NGAY khi chọn file (thiếu xem trước→xác nhận) + xlsx CVE + không giới hạn dòng [FIXED]
- **Phát hiện bởi:** QA-pair đối kháng (code-reviewer độc lập) trong review #9 (nhập liệu hàng loạt), TRƯỚC khi LEAD commit — không lọt tới người dùng. Backend import (quyền đôi lớp / tái dùng create thật / cô lập dòng / FK không đoán) đã PASS; lỗ nằm ở tầng FE + dependency.
- **Nguyên nhân:** (1) HIGH — `ImportModal` chọn file → parse → gọi thẳng `import:run` TẠO NGAY, chỉ hiện kết quả hậu-kiểm. Với phiếu Thu/Chi (không khóa tự nhiên chống trùng) → chọn nhầm file / import lại = **nhân đôi bản ghi tài chính** (chỉ gỡ được bằng hủy từng phiếu + mật khẩu). (2) MEDIUM — `xlsx@0.18.5` (bản cuối trên npm) dính CVE-2023-30533 prototype-pollution + CVE-2024-22363 ReDoS, đúng bề mặt parse file người dùng. (3) MEDIUM — không giới hạn số dòng → file 50k dòng đông cứng main process + tạo 50k bản ghi.
- **Fix:** (1) tách luồng **dry-run → xem trước → xác nhận → tạo**: thêm `import:dryRun` (validate + tra FK + toCreateInput, KHÔNG create, trả validCount/invalidCount) dùng chung `gateAndPrepare` với `runImport`; `ImportModal` hiện bảng xem trước + nút "Nhập N dòng hợp lệ" (disabled khi N=0) rồi mới `import:run`. (2) chuyển `xlsx` → bản SheetJS maintained `https://cdn.sheetjs.com/xlsx-0.20.3` (vá cả 2 CVE; npm registry chỉ có ≤0.18.5) — bundle inline, không CDN runtime/CSP. (3) `MAX_IMPORT_ROWS=2000` chặn ở `gateAndPrepare` (backend authoritative) + `parseWorkbook` (defense-in-depth) → `TOO_MANY_ROWS`.
- **Regression:** selftest 31 (import) +8 ca (39→47): dryRun trả validCount=2/invalidCount=1 + **KHÔNG tạo bản ghi (count trước===sau)**; run/dryRun vượt trần → TOO_MANY_ROWS + không tạo; **cách ly chéo quyền** (WAREHOUSE có CONFIG_TID_MANAGE không CASHENTRY_CREATE → tid created / cashThu FORBIDDEN cả run lẫn dryRun).
- **Đề xuất quy trình (bug class "thao tác ghi hàng loạt tài chính thiếu bước xác nhận"):** MỌI luồng tạo/ghi HÀNG LOẠT (đặc biệt tài chính) PHẢI có bước dry-run/xem-trước→xác nhận, KHÔNG ghi thẳng khi người dùng vừa chọn nguồn. Selftest phải phủ tầng FE-flow (dry-run không tạo), không chỉ logic backend. Dependency parse file người dùng phải là bản đã vá CVE. Nối [[feedback_ui_test_gap_paired_qa_loop_10_7]].

### B22 — Gán TID lên máy ĐÃ CÓ TID khác → mồ côi TID + 2 binding mở [FIXED]
- **Phát hiện bởi:** QA-pair đối kháng (agent code-reviewer độc lập) trong vòng review PHASE_K K2, TRƯỚC khi LEAD commit — không lọt tới người dùng. Ảnh hưởng CẢ `createTidUnified` (K2, code mới) LẪN `assignTid` (K1 đã commit+tag v0.22.0-k1 — defect tiềm ẩn đã ship trong tag).
- **Nguyên nhân:** `assignTid`/`createTidUnified` chỉ kiểm `posDevice.status==='RETIRED'` rồi set `posDevice.currentTid = tid` VÔ ĐIỀU KIỆN. Nếu máy đích đang gắn 1 TID ACTIVE khác (`currentTid` đã set) → TID cũ vẫn giữ `posSerial` trong khi máy trỏ TID mới → **mồ côi**, thêm 2 dòng `pos_tid_binding` `unboundAt=null`. `SELECT ... FOR UPDATE` có khóa hàng nhưng KHÔNG re-kiểm `currentTid` → bất biến "1 máy 1 TID sống" không được bảo vệ. UI che đường thường (picker chỉ liệt kê máy IN_STOCK) nhưng IPC trực tiếp / UI cũ / tương tranh vẫn kích hoạt được.
- **Fix:** trong CẢ HAI hàm, sau FOR UPDATE re-đọc device trong `$transaction`: nếu `dev.currentTid != null && dev.currentTid !== tid` → `DEVICE_HAS_TID` (rollback, thông báo "Máy X đang gắn TID Y, thu hồi trước khi gán TID khác"). Chỉ gán khi máy chưa có currentTid. Kèm FIX phụ (cùng review K2): validate tồn tại customerId/agentId khi assign/deliver (Int? không FK → id bừa lưu âm thầm); kẹp occurredAt sự kiện Giao ≥ Gán (chống đảo timeline khi deliveredAt lùi ngày); thay non-null assertion `dev!.id` bằng NOT_FOUND thân thiện.
- **Regression:** selftest 30 (tidunify) — 2 TID cùng gán 1 máy → cái thứ 2 chặn DEVICE_HAS_TID, máy giữ TID đầu, 1 binding mở; deliver.customerId/toAgentId=999999 → NOT_FOUND; timeline TID_ASSIGN trước TID_DELIVERED. selftest 29 (posunify) — back-port cho `assignTid`.
- **Đề xuất quy trình (bug class "bất biến 1-1 không được re-kiểm dưới khóa"):** MỌI thao tác set khóa ngoại "hiện đang trỏ" (currentTid, currentCustomerId...) trong `$transaction` có FOR UPDATE PHẢI re-kiểm giá trị cũ dưới khóa trước khi ghi đè — FOR UPDATE chỉ chống tương tranh, KHÔNG tự bảo vệ bất biến nếu code không kiểm. Đây là lý do CẶP AGENT QA ĐỘC LẬP review diff trước commit (vòng lặp Mr.Long lock 10/7): defect này AUDIT tự-rerun selftest KHÔNG bắt (selftest cũ không có ca gán-đè), chỉ QA đối kháng đọc code mới thấy. Nối [[feedback_ui_test_gap_paired_qa_loop_10_7]].

### B21 — Seed trạng thái nhập kho POS phân biệt hoa/thường → đẻ bản GẦN TRÙNG ("Máy mới" cạnh "Máy Mới" của admin) [FIXED]
- **Phát hiện bởi:** LEAD 11/7 — khi nâng DB thật `glb` lên đủ migration rồi chạy `seedIfEmpty` (server role) để tiến hóa quyền/danh mục cho lần reopen của Mr.Long, dropdown trạng thái nhập kho xuất hiện 2 dòng gần trùng: "Máy Mới" (admin đã nhập tay, created_by=1) và "Máy mới" (seed đẻ, created_by=NULL).
- **Nguyên nhân:** `seedDefaultIntakeStatusesIfMissing` (db.ts) kiểm tồn tại bằng `findUnique({ where: { name } })` — khớp CHÍNH XÁC, phân biệt hoa/thường. DB cũ của Mr.Long đã có "Máy Mới" (hoa) nên seed không thấy khớp "Máy mới" (thường) → tạo thêm bản gần trùng. Lớp bug **db-evolution-gap**: hàm seed idempotent theo khóa tự nhiên nhưng khóa so khớp không chuẩn-hóa hoa/thường, mâu thuẫn với lô Polish (đã chuẩn-hóa mọi lọc sang không phân biệt hoa/thường). Điểm mù: selftest 5 chạy DB throwaway (seed lần đầu tạo đúng "Máy mới") nên không tái hiện kịch bản "DB đã có biến thể hoa".
- **Fix:** đổi sang `findFirst({ where: { name: { equals: name, mode: 'insensitive' } } })` — nếu đã có BẤT KỲ biến thể hoa/thường (kể cả xóa mềm) thì bỏ qua, tôn trọng dữ liệu admin, không đẻ trùng. Dọn dữ liệu `glb`: soft-delete dòng "Máy mới" (created_by=NULL, chưa có intake tham chiếu), GIỮ "Máy Mới" của admin.
- **Regression (chặn tái diễn):** selftest 5 (GCFG5) thêm khối CUỐI: xóa sạch trạng thái nhập → tạo trước "Máy Mới" (hoa) mô phỏng DB cũ → gọi `seedDefaultIntakeStatusesIfMissing` → assert (a) chỉ tạo 3 (cũ/đổi/thuê), (b) KHÔNG có "Máy mới" thường, (c) giữ nguyên "Máy Mới", (d) tổng đúng 4, (e) seed lần 2 = no-op. pass 107→112.
- **Đề xuất quy trình (bug class db-evolution-gap):** MỌI hàm seed/idempotent-theo-tên PHẢI so khớp KHÔNG phân biệt hoa/thường (nhất quán với lọc); và test tiến-hóa-DB phải mô phỏng DB ĐÃ CÓ biến thể của khóa tự nhiên (không chỉ DB rỗng). Nối [[feedback_verify_before_claim_and_db_upgrade_gap_9_7]].

### B20 — Phục hồi (restore) trên Postgres làm MẤT bản ghi backup an-toàn (pre-restore snapshot) khỏi `backup_logs` [FIXED]
- **Phát hiện bởi:** Mr.Long 10/7 — sau khi Phục hồi, bản sao lưu hiện trạng (tạo tự động TRƯỚC restore để undo) biến mất khỏi danh sách backup → không còn đường tra cứu/hoàn tác về trạng thái trước restore.
- **Nguyên nhân:** `restoreBackup` (backup-service.ts) gọi `writeBackupArchive(..., 'auto pre-restore snapshot')` → ghi 1 dòng `backup_logs` (R_BACKUP_003), rồi chạy `pg_restore --clean --if-exists --single-transaction` nạp dump ĐẦY ĐỦ (gồm cả bảng `backup_logs`). `--clean` DROP+CREATE lại `backup_logs` rồi nạp các dòng CŨ của dump → dòng pre-restore snapshot vừa ghi BỊ GHI ĐÈ MẤT (backupsAfterRestore=0 trong khi backupsBeforeRestore=1). File ZIP an-toàn VẪN còn trên đĩa, chỉ mất BẢN GHI log nên `listBackups` không thấy → không undo được. Lớp bug: "dữ liệu do chính thao tác ghi TRƯỚC khi restore bị dump ghi đè".
- **Fix:** (1) `writeBackupArchive` trả thêm `logRow` (metadata dòng backup_logs vừa tạo: filePath/fileSize/checksum/createdBy/note/createdAt). (2) Trong `restoreBackup`, SAU khi `pg_restore` thành công (trước audit RESTORE_EXECUTED) → RE-INSERT lại dòng pre-restore snapshot bằng `db.backupLog.create` với metadata đã capture (giữ nguyên `createdAt`) → bản sao lưu an-toàn lại xuất hiện trong `listBackups` (backupsAfterRestore ≥1). Prisma client vẫn hoạt động sau pg_restore (schema y hệt, chỉ dữ liệu đổi). Audit RESTORE_EXECUTED ghi SAU pg_restore nên KHÔNG bị mất (giữ nguyên). `requirePermission` chỉ ghi audit ở nhánh TỪ CHỐI (không ghi ở nhánh thành công) nên không có bản ghi nào khác bị mất tương tự.
- **Regression (chặn tái diễn):** SELFTEST2 (phaseb) R_BACKUP_003 sửa từ `backupsAfterRestore > backupsBeforeRestore` (assert SAI/mù — thực ra fail vì dòng bị mất) sang: `backupsAfterRestore >= 1` **VÀ** bản ghi mới nhất (`data[0]`, order id desc) có `note === 'auto pre-restore snapshot'` **VÀ** `exists === true` (file khớp trên đĩa). Bắt đúng lớp bug: sau restore phải còn tra cứu được pre-restore snapshot.
- **Đề xuất quy trình (bug class = "restore ghi đè dữ liệu do chính thao tác ghi trước đó" + gap kiểm thử hạ tầng):** (a) MỌI bản ghi CẦN sống-sót qua restore mà được ghi TRƯỚC `pg_restore --clean` phải hoặc ghi SAU restore, hoặc capture-rồi-re-insert sau restore (dump là ảnh chụp quá khứ → mọi ghi mới hơn dump đều bị cuốn trôi). (b) **AUDIT bắt buộc chạy FULL selftest suite sau mỗi thay đổi hạ tầng** (đây là điểm mù thật: bug lọt từ G10.2 khi chuyển SQLite→Postgres vì KHÔNG chạy lại selftest 2 — selftest cũ đã có assert R_BACKUP_003 nhưng không được rerun trên Postgres nên không phơi ra pg_restore --clean wipe). Migrate provider = chạy lại TOÀN BỘ selftest, không chỉ selftest của module mới đụng.

### B19 — Admin đặt lại MK user khác KHÔNG re-auth actor → chiếm tài khoản qua phiên bỏ ngỏ [FIXED]
- **Phát hiện bởi:** Mr.Long 10/7 — lỗ hổng bảo mật: `adminResetPassword` chỉ kiểm `requirePermission('USER_RESET_PASSWORD')`, KHÔNG bắt actor xác nhận lại mật khẩu của chính mình. Ai ngồi vào máy admin đang mở phiên (bỏ ngỏ, không khóa màn hình) đều reset được mật khẩu BẤT KỲ user → đăng nhập chiếm tài khoản.
- **Nguyên nhân:** thao tác nhạy cảm trên tài khoản NGƯỜI KHÁC (đặt lại mật khẩu) thiếu bước re-auth actor — trong khi thao tác XÓA user (`deleteUser`/`deleteUsers`) ĐÃ có `verifyActorPassword` (§14). Bất đối xứng: xóa cần nhập lại mật khẩu, reset thì không, dù reset còn nguy hiểm hơn (dẫn tới chiếm quyền đăng nhập).
- **Fix:** thêm tham số `actorPassword` vào `adminResetPassword(targetUserId, newPassword, actorPassword)`; sau `requirePermission` gọi `verifyActorPassword(actor, actorPassword)` — SAI → return `{ok:false, error:'WRONG_ACTOR_PASSWORD', message:'Mật khẩu của bạn không đúng — không thể đặt lại mật khẩu người dùng.'}`, ghi audit `PERMISSION_DENIED` (reason WRONG_ACTOR_PASSWORD), KHÔNG đổi gì DB của target. Tái dùng đúng pattern `verifyActorPassword` như thao tác xóa (KHÔNG tính vào bộ đếm khóa — nhất quán delete). Wire xuống `ipc.ts` + `preload/index.ts` + `index.d.ts`; UI `AdminResetPasswordModal` thêm ô "Mật khẩu của bạn (xác nhận)" dùng `PasswordInput` (con mắt đồng bộ), bắt buộc.
- **Regression (chặn tái diễn):** selftest NHOMA11 khối E thêm 3 assert — (a) actorPassword SAI → `WRONG_ACTOR_PASSWORD` + hash mật khẩu target GIỮ NGUYÊN + target vẫn LOCKED/`forceChangePassword=false` (chứng minh 0 thay đổi DB); (b) actorPassword ĐÚNG → reset OK + `forceChangePassword=true` + mở khóa (giữ nguyên assert cũ). Cập nhật 2 lệnh gọi `adminResetPassword` cũ trong selftest theo chữ ký mới.
- **Đề xuất quy trình (bug class = "thao tác nhạy cảm trên tài khoản người khác thiếu re-auth"):** MỌI thao tác quản trị làm thay đổi khả năng đăng nhập / quyền của TÀI KHOẢN NGƯỜI KHÁC (reset MK, mở khóa, đổi trạng thái, gán quyền cao) PHẢI re-auth actor bằng `verifyActorPassword` giống thao tác xóa — không dựa duy nhất vào phiên đang mở. Điểm mù cũ: chỉ xóa được audit + re-auth; reset bị bỏ sót vì selftest chỉ test đường thành công (không có ca "phiên hợp lệ + không xác nhận actor").

### PF-01 — Auto-update .exe bị BỎ SÓT do "duyệt gộp chôn việc lớn" [FIXED-PROCESS]
- **Phát hiện bởi:** Mr.Long 10/7 — "cập nhật hệ thống nằm ở đâu? user khác tải bản cập nhật kiểu gì?" → truy ra app KHÔNG có cơ chế cập nhật.
- **Nguyên nhân (quy trình, KHÔNG phải code):** `PHASE_G10_DEPLOYMENT_SPEC.md` Q5 = *"Auto-update .exe | HOÃN — cài tay bản mới"* do **LEAD tự đặt mặc định** rồi **gộp chung vào lô "chốt Q1–Q6"**. Mr.Long gật cả cụm → một hạng mục LỚN (đụng triển khai đa máy) bị **quyết ngầm trong lô**, không được đưa ra hỏi riêng. Lỗi thuộc LEAD: nhét việc lớn vào dòng mặc định của lô duyệt.
- **Fix:** mở lại D01, build G11 (electron-updater tích hợp, spec `PHASE_G11_AUTOUPDATE_SPEC.md`).
- **Regression (chặn tái diễn) — HARDLOCK QUY TRÌNH:** (a) lập `docs/DEFERRED_REGISTRY.md` — mọi ô HOÃN/TODO/tạm trong `docs/*_SPEC.md` PHẢI khai báo 1 dòng trong sổ (id + lý do + điều kiện mở lại); (b) **guard** `tools/audit/deferred_guard.mjs` quét token HOÃN/TODO chưa khai báo → fail (thêm vào `verify` + pre-commit); (c) **luật LEAD:** CẤM gói ≥2 quyết định lớn vào 1 lời "chốt"; việc đụng hạ tầng/đa máy/bảo mật/dữ liệu phải hỏi 1 câu RIÊNG, duyệt riêng. **Bài học:** "chốt" một lô ≠ Mr.Long đã cân nhắc từng dòng — LEAD chịu trách nhiệm phơi bày việc lớn, không để trôi theo mặc định.

### B16 — Ngày hiệu lực biểu phí LỆCH −1 NGÀY trên máy UTC+7 (production) [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (audit P1.1 giá theo kỳ) — máy production chạy UTC+7. User nhập "Hiệu lực từ = 01/07/2026" nhưng bảng biểu phí hiển thị/lưu **30/06/2026**.
- **Nguyên nhân:** `setFeeRate` floor `effectiveFrom` theo **UTC-day** (`startOfDayUtc` = `Date.UTC(getUTCFullYear/Month/Date)`), trong khi CẢ app còn lại (`txnDate`, filter `dateFrom/dateTo`) lưu **nguyên instant nửa-đêm-LOCAL** do UI dựng `new Date(d+'T00:00:00').toISOString()` (không `Z`), và `fmtDate` round-trip bằng **getter LOCAL**. Bất đối xứng đúng bằng offset +7h: UI gửi 01/07 local (= `2026-06-30T17:00Z`) → `startOfDayUtc` floor theo UTC-day về `2026-06-30T00:00Z` → `fmtDate` (local) hiện 30/06. Tiền doanh thu vẫn ĐÚNG (2 vế `pickEffectiveRate`/txnDate cùng so instant), nhưng NGÀY sai → vi phạm R_DATE_FORMAT (LEAD lock 9/7).
- **Fix:** thay `startOfDayUtc` bằng `startOfDayLocal(d)` = `new Date(d.getFullYear(), d.getMonth(), d.getDate())` — chuẩn hóa về nửa-đêm-LOCAL, đối xứng với `fmtDate` và `txnDate`. Sau sửa: nhập 01/07 → lưu instant nửa-đêm 01/07 local → `fmtDate` hiện "01/07/2026"; GD ngày 01/07 ăn đúng kỳ, GD ngày 30/06 KHÔNG ăn kỳ 01/07. KHÔNG đụng `pickEffectiveRate` (thuần, đã đúng), dedup 24h, `isCurrent`, snapshot, schema/migration.
- **Regression (chặn tái diễn):** REV15 khối mới **L) GIÁ THEO KỲ — ĐƯỜNG UI** (parse-LOCAL, KHÔNG `Z`, tổ hợp/tid riêng): set kỳ `effectiveFrom = new Date('2026-08-01T00:00:00').toISOString()` (như UI gửi) → `listFeeRates` → assert **`fmtDate(dto.effectiveFrom) === '01/08/2026'`** (chứng cứ chạy thật: `iso=2026-07-31T17:00:00.000Z` = nửa-đêm 01/08 trên UTC+7, hiển thị đúng 01/08). Kèm: GD `txnDate` local 2026-08-01 ăn kỳ 01/08 (margin 4000/3000); GD local 2026-07-31 KHÔNG ăn, rơi về kỳ 01/07 (margin 2000/1500). REV15 pass=73 fail=0.
- **Đề xuất quy trình (bug class = "ngày lệch do trộn UTC-day / LOCAL-day"):** mọi test liên quan **hiển thị/so sánh NGÀY** PHẢI có ≥1 ca đi qua **đường parse-LOCAL** (ISO KHÔNG `Z`, đúng như UI gửi), KHÔNG chỉ ISO `Z` (UTC thuần). Điểm mù cũ: toàn bộ selftest + unit dùng ISO có `Z` nên không đi qua đường UI parse-local → bug lọt trên máy UTC+7. Mọi chuẩn-hóa "về đầu ngày" phải đối xứng với hàm hiển thị (`fmtDate` dùng getter LOCAL ⇒ floor phải LOCAL).

### B10 — `updateTransaction` tra lại phí "giá hôm nay" khi chỉ sửa note/ngày/số tiền → PHÁ snapshot doanh thu đã khóa [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (agent phản biện doanh thu #1) — sửa ghi chú 1 giao dịch cũ khiến doanh thu bị tính lại theo biểu phí HIỆN TẠI, làm sai sổ đã chốt.
- **Nguyên nhân:** `updateTransaction` luôn `resolveFeeForTxn` rồi ghi đè `partnerMarginMilli/sellMarginMilli` bất kể trường nào đổi. Vi phạm nguyên tắc snapshot (phí phải đóng băng vào từng GD; đổi bảng phí sau này KHÔNG được đổi doanh thu đã ghi).
- **Fix:** chỉ tra lại phí khi `cardTypeId` thực sự đổi (`cardChanged`); mọi trường khác giữ nguyên margin snapshot của bản ghi. `computeRevenue` chạy lại từ margin đã khóa.
- **Regression (chặn tái diễn):** REV15 thêm 2 assert — (a) sửa note/ngày → `revenuePartner/Sell/Amount` KHÔNG đổi; (b) đổi bảng phí rồi sửa note → doanh thu vẫn bằng snapshot cũ. **Đề xuất quy trình:** mọi service ghi số tiền dẫn xuất phải có test "sửa trường không-liên-quan → số tiền bất biến".

### B11 — Lọc doanh thu theo TID dùng `deletedAt:null` → giao dịch của TID đã xóa mềm biến mất khỏi báo cáo [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (agent phản biện doanh thu #2) — TID bị xóa mềm thì toàn bộ doanh thu lịch sử của nó rơi khỏi tổng hợp → sai doanh thu/công nợ.
- **Nguyên nhân:** `buildWhere` resolve TID theo tid-string kèm điều kiện `deletedAt: null`; TID xóa mềm không match → where TID rỗng → loại hết GD của TID đó.
- **Fix:** bỏ `deletedAt:null` khỏi `tidWhere` (GD gắn `tidId` cứng, không phụ thuộc trạng thái sống/chết của TID). Báo cáo phản ánh đủ doanh thu lịch sử.
- **Regression (chặn tái diễn):** REV15 thêm assert — xóa mềm 1 TID rồi lọc theo tid-string đó vẫn trả đúng GD + summary. **Bài học:** báo cáo tài chính lấy theo khóa bất biến (`tidId`), không theo trạng thái hiện tại của thực thể tham chiếu.

### B12 — Cấu hình lưu trữ cho phép hạn giữ dưới sàn an toàn → dọn dẹp có thể xóa dữ liệu quá sớm [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (agent phản biện storage) — cấu hình `auditRetentionDays=0`/`trashRetentionDays=0` khiến purge có thể xóa audit/thùng rác ngay lập tức, mất dấu vết.
- **Nguyên nhân:** `runCleanup` dùng thẳng giá trị cấu hình làm mốc cắt, không có sàn tối thiểu; `updateStorageConfig` không validate cận dưới.
- **Fix:** hằng sàn `MIN_AUDIT_DAYS=7`, `MIN_TRASH_DAYS=1`, `MIN_BACKUP_HOURS=1`; helper `auditRetention/trashRetention/backupInterval` = `Math.max(config, floor)` ở MỌI điểm tính mốc cắt; `updateStorageConfig` reject giá trị dưới sàn (VALIDATION).
- **Regression (chặn tái diễn):** STG16 thêm assert — set retention=0 bị reject; purge với cấu hình hợp lệ vẫn tôn trọng sàn (không xóa bản ghi mới hơn sàn). **Đề xuất quy trình:** mọi tham số điều khiển thao tác phá hủy phải có sàn an toàn code-enforced, không tin cấu hình.

### B13 — Health-Scan tự sửa doanh thu KHÔNG backup trước → mất bản gốc nếu công thức sai [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (agent phản biện health-scan) — autoFix ghi đè `revenue*` mà chưa có bản sao lưu → nếu logic recompute sai thì không hoàn tác được.
- **Nguyên nhân:** `runScan({autoFix:true})` gọi `applyAutoFixes` trực tiếp, không backup trước.
- **Fix:** `runScan` gọi `systemBackup` TRƯỚC `applyAutoFixes`; backup lỗi → ABORT, không tự sửa (an toàn dữ liệu ưu tiên tuyệt đối).
- **Regression (chặn tái diễn):** HSC17 assert — sau autoFix, `backup_logs` +1 (backup xảy ra TRƯỚC khi ghi đè). **Bài học:** mọi thao tác tự-sửa hàng loạt phải backup-trước-abort-nếu-lỗi.

### B14 — Trash: xóa vĩnh viễn/dọn sạch bị TỪ CHỐI (sai mật khẩu, chưa đặt cấp 2) KHÔNG ghi audit → mất dấu vết ý đồ phá hoại [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (agent phản biện bảo mật) — thao tác phá hủy bị chặn nhưng im lặng, vi phạm R_AUDIT_003 (thao tác phá hủy phải ghi audit KỂ CẢ khi bị từ chối).
- **Nguyên nhân:** `purgeItem`/`emptyTrash` return sớm ở nhánh sai mật khẩu / `LEVEL2_NOT_SET` / `WRONG_LEVEL2` mà không `writeAudit`.
- **Fix:** thêm `writeAudit(..., after:{denied:true, reason})` ở cả 3 nhánh từ chối (WRONG_PASSWORD / LEVEL2_NOT_SET / WRONG_LEVEL2).
- **Regression (chặn tái diễn):** TRASH6 (selftest =6) thêm khối FIX5 — mỗi nhánh từ chối làm `audit_logs` +1 (afterJson chứa reason), bản ghi VẪN còn (không xóa cứng), thùng rác KHÔNG bị xóa. **Đây là ca selftest cũ bỏ sót** (chỉ test đường thành công). **Đề xuất quy trình:** mọi handler thao tác phá hủy phải có test "đường từ chối cũng ghi audit".

### B15 — Trang Công nợ giới hạn 500 bản ghi âm thầm (không phân trang) → tổng hợp thiếu khi >500 GD chưa thu [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (agent phản biện doanh thu #3) — DebtPage tải tối đa 500 dòng, không phân trang, tổng ở tfoot chỉ cộng trang đang thấy → sai công nợ khi vượt ngưỡng.
- **Nguyên nhân:** DebtPage thiếu state phân trang; summary tính từ danh sách bị cắt.
- **Fix:** thêm `page/total/pageSize` + `reload(pg)` phân trang; summary công nợ lấy từ aggregate TOÀN BỘ tập lọc (service-side), không từ trang hiển thị.
- **Regression (chặn tái diễn):** REV15/debtSummary assert summary cộng trên toàn tập lọc, độc lập phân trang. **Bài học:** mọi tổng hợp phải tính server-side trên full filtered set, không phải trên trang UI (cùng lớp với "silent cap" — luôn `log`/hiển thị khi cắt).

### B08 — DTO giao dịch select `customer.name` (không tồn tại) → PrismaClientValidationError khi liệt kê doanh thu [FIXED]
- **Phát hiện bởi:** CMD_BUILD khi tự chạy selftest =15 (REV15) — dừng ngay sau khi tạo GD2, `listTransactions` ném `PrismaClientValidationError` (không in ra được danh sách). Bắt TRƯỚC khi báo cáo (đúng R9 evidence-first).
- **Nguyên nhân:** `transaction-service.listTransactions` resolve tên khách bằng `db.customer.findMany({ select: { id, name } })` — model `Customer` KHÔNG có cột `name` (chỉ `fullName` + `nickname` + `code`). Prisma validate select ở tầng client → ném lỗi runtime.
- **Fix:** đổi sang `select: { id, fullName }` và map `customerName = fullName`. Selftest REV15 xác nhận DTO trả `customerName = "Khách Doanh Thu"` đúng.
- **Regression (chặn tái diễn):** [ĐÃ có] REV15 assert DTO có nhãn khách/loại thẻ/HKD/MID → bắt đúng lớp bug này. **Đề xuất quy trình:** esbuild của electron-vite KHÔNG typecheck đầy đủ → sai tên cột Prisma `select` lọt build. BẮT BUỘC chạy `npm run typecheck` (tsc `-p tsconfig.node.json`) TRƯỚC khi coi backend "build OK" — Prisma client typed sẽ bắt `select` sai tên cột ở compile-time. CMD_AUDIT thêm bước: chạy typecheck node+web = 0 lỗi là gate bắt buộc.

### B09 — Selftest treo/ báo "table does not exist" do (a) process schema-engine/electron kẹt + (b) path MSYS `/c/` vs Windows `C:/` lệch [FIXED — test infra]
- **Phát hiện bởi:** CMD_BUILD khi chạy selftest =17 — `prisma migrate deploy` timeout 60-120s; electron báo `table main.permissions does not exist` dù DB vừa deploy xong.
- **Nguyên nhân kép:** (1) Các lần selftest bị timeout để lại process `schema-engine-windows.exe` + `electron.exe` treo, GIỮ LOCK trên DB/migrations → mọi `migrate deploy` sau đó bị chặn (SocketTimeout). (2) `migrate deploy` dùng `DATABASE_URL='file:/c/Users/ADMINI~1/...'` (path MSYS) còn electron dùng `GLB_DB_URL='file:C:/Users/Administrator/...'` (path Windows) → Prisma tạo bảng ở MỘT file, electron mở file KHÁC (rỗng) → "table does not exist". Trước đây trùng path MSYS nên vô tình khớp.
- **Fix:** (a) trước mỗi đợt selftest `taskkill /F /IM electron.exe` + `taskkill /F /IM schema-engine-windows.exe`; (b) DÙNG CÙNG dạng path Windows `C:/...` cho CẢ `migrate deploy` LẪN `GLB_DB_URL`, hoặc copy sẵn `dev.db` (đã checkpoint WAL) sang file tạm rồi trỏ GLB_DB_URL path Windows. Sau fix: REV15 43/0, STG16 33/0, HSC17 22/0, fresh-deploy 16/0.
- **Regression (chặn tái diễn):** **Quy trình chuẩn chạy selftest (bổ sung B04):** 1) kill electron + schema-engine trước; 2) path Windows `C:/...` đồng nhất giữa migrate & GLB_DB_URL (hoặc copy dev.db đã checkpoint); 3) `dev.db` phải `PRAGMA wal_checkpoint(TRUNCATE)` trước khi copy (WAL split làm copy thiếu bảng). **Bài học:** treo test = thất bại hạ tầng test, không phải lỗi sản phẩm — nhưng phải dọn process + đồng nhất path để kết quả lặp lại được.

### B07 — Thứ tự migration sai: folder timestamp TRƯỚC bảng phụ thuộc → fresh deploy fail [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (selftest =13 fresh-deploy) — chạy trên DB throwaway MỚI báo PrismaClientKnownRequestError; chỉ 2 assert đầu pass rồi crash.
- **Nguyên nhân:** migration `deletedBy` được `prisma migrate dev --create-only` sinh timestamp `20260709165229`, NHỎ HƠN gcfg4/5/6 (`170000/180000/190000`). Trên dev.db (đã áp mọi migration) thì deploy chỉ thêm migration cuối → OK. Nhưng trên DB MỚI, deploy áp theo THỨ TỰ tên folder → `deletedBy` (165229) chạy TRƯỚC khi gcfg4/5/6 tạo bảng `dossiers`/`receive_accounts`/`tid_config_statuses` → `ALTER TABLE ... ADD COLUMN deleted_by` trên bảng CHƯA tồn tại → fail. Cùng lớp với B02 (migration history drift).
- **Fix:** đổi tên folder → `20260709200000_nhoma_deletedby_per_user_trash` (sau MỌI migration). Dựng lại dev.db fresh để history khớp.
- **Regression (chặn tái diễn):** [ĐÃ có] selftest fresh-deploy (migrate deploy sang DB throwaway TRỐNG) bắt đúng lớp bug này — mọi selftest N≥2 đều chạy fresh deploy. **Đề xuất quy trình:** sau mỗi `migrate dev --create-only`, PHẢI kiểm tra timestamp folder > mọi migration nó phụ thuộc (bảng bị ALTER phải được tạo ở migration có timestamp NHỎ HƠN). Nếu migration đụng bảng của feature mới hơn → rename folder cho đúng thứ tự.

### B06 — Seed KHÔNG cấp quyền MỚI cho role đã tồn tại → ADMIN thiếu quyền → menu bị ẩn [FIXED]
- **Phát hiện bởi:** LEAD (Mr.Long) khi mở app thấy toàn bộ menu Cấu hình biến mất dù code đủ.
- **Nguyên nhân:** `db.ts seedIfEmpty` upsert permission mới vào bảng `permissions` NHƯNG chỉ gán `role_permissions` cho role VỪA TẠO (fix G-POS-A01 giữ chỉnh tay admin). Role ADMIN có từ Phase A không nhận quyền CONFIG_* mới → kẹt 29/43 → `hasAnyPermission` ẩn menu.
- **Vì sao test không bắt:** MỌI selftest chạy DB throwaway MỚI → role tạo mới → quyền tự seed → ADMIN đủ → PASS. Chưa mô phỏng ca thật "DB đã tồn tại + thêm feature thêm permission" (đường nâng cấp/production). Đúng R196 Engineering PASS ≠ Production PASS.
- **Fix:** thêm khối `R_ADMIN_SUPERUSER` trong seedIfEmpty — ADMIN = superuser, LUÔN đồng bộ ĐỦ mọi permission mỗi boot (role khác giữ chỉnh tay). Bằng chứng nâng cấp thật: adminroot trên dev.db CŨ tự lên 43→48 quyền sau khi thêm 5 permission Nhóm A.
- **Regression (chặn tái diễn):** [ĐÃ có 1 phần] mọi permission mới sẽ tự tới ADMIN. **CÒN NỢ (đề xuất quy trình):** thêm test class "DB tiến hóa/upgrade" — seed DB cũ (thiếu permission X) → thêm X → gọi seedIfEmpty → assert ADMIN nhận đủ X. Ca này bắt đúng bug; selftest hiện chỉ chạy DB fresh nên chưa cover. Ghi nợ để slice sau bổ sung.

### B01 — Schema Phase A thiếu cột `join_date` (§9 "Ngày vào làm") [FIXED]
- **Phát hiện bởi:** CMD_BUILD khi re-đọc IMS_SPEC §9 lúc dựng form nhân sự Phase B.
- **Nguyên nhân:** Phase A dựng schema 9 bảng nhưng bỏ sót 1 trường optional của form user (§9). Quy trình test Phase A
  chỉ kiểm auth/username — KHÔNG có test đối chiếu đủ trường form §9 với cột DB → lọt.
- **Fix:** thêm `users.joinDate` vào `schema.prisma` + migration `20260709120000_add_user_join_date` + ALTER dev.db
  (qua Electron better-sqlite3) + wire vào CreateUserInput/UpdateUserInput/UserDto + form StaffPage.
- **Regression (chặn tái diễn):** đề xuất CMD_AUDIT thêm 1 test "schema-vs-spec field coverage" — assert mọi trường
  bắt buộc/§9 form user có cột DB tương ứng. Tạm thời: self-test tạo user với đủ trường (birthDate/joinDate) đã cover
  path persist. **Đề xuất quy trình:** mỗi khi thêm form field theo spec, BẮT BUỘC đối chiếu schema trước khi đóng phase.

<!-- Không phát hiện bug logic khác trong Phase B. Self-test 24/24 + Vitest 61/61 PASS. -->

### B02 — Migration history drift: `join_date` không ghi trong `_prisma_migrations` [FIXED]
- **Phát hiện bởi:** CMD_BUILD khi chạy `prisma migrate dev` cho G-POS → Prisma báo "Drift detected" đòi reset DB (mất data).
- **Nguyên nhân:** B01 (Phase B) ALTER dev.db trực tiếp qua Electron better-sqlite3, KHÔNG ghi migration folder vào bảng `_prisma_migrations`. History chỉ có `init` → Prisma coi cột `join_date` là drift thủ công.
- **Fix:** KHÔNG reset (giữ data). `prisma migrate resolve --applied 20260709120000_add_user_join_date` để đánh dấu đã áp dụng, rồi hand-write migration G-POS + `prisma migrate deploy` (additive, không reset).
- **Regression (chặn tái diễn):** **Đề xuất quy trình:** cấm ALTER DB thủ công ngoài `prisma migrate` — mọi thay đổi schema PHẢI qua migration folder + `migrate deploy`/`dev` để history đồng bộ. Nếu buộc ALTER tay, PHẢI `migrate resolve` ngay để ghi history. CMD_AUDIT nên thêm check "migrations folder count == _prisma_migrations rows".

### B03 — Self-test G-POS assert sai thứ tự event do occurredAt backdate không đồng nhất [FIXED]
- **Phát hiện bởi:** CMD_BUILD khi chạy GLB_SELFTEST=3 lần 1 (1/40 FAIL "event types in correct order").
- **Nguyên nhân:** test backdate occurredAt cho 4 transition (2026-07-01..04) nhưng để `createPos` (STOCK_IN) = "now" (2026-07-09). `getDeviceTimeline` sort đúng theo occurredAt (thời gian thao tác thực) nên STOCK_IN rớt xuống cuối — **code đúng, giả định test sai**.
- **Fix:** test truyền `occurredAt` sớm hơn cho createPos (2026-06-01) để dữ liệu nhất quán. KHÔNG sửa code sản phẩm (event-sourcing sort-by-occurredAt là đúng thiết kế §A1).
- **Regression (chặn tái diễn):** self-test đã lock thứ tự 5 event (STOCK_IN→DEPLOY→REPORT_DAMAGE→SEND_REPAIR→RECEIVE_REPAIRED) + assert mọi event có occurredAt hợp lệ. **Bài học:** khi test event-sourced timeline, occurredAt của MỌI event (kể cả create) phải set nhất quán chronological.

### G-POS-A01 — Seed re-sync âm thầm hoàn quyền admin đã gỡ [FIXED — LEAD lock 9/7]
- **Phát hiện bởi:** CMD_AUDIT (đọc `db.ts::seedIfEmpty`). **Luật LEAD 9/7:** *mọi thao tác/sửa đổi ghi log realtime, không xóa được; app KHÔNG tự ý hoàn tác/đổi dữ liệu.*
- **Nguyên nhân:** `seedIfEmpty` upsert `DEFAULT_ROLE_PERMISSIONS` mỗi lần boot với `update:{}` → nếu admin gỡ 1 quyền default khỏi role, reboot `create`-lại quyền đó, **âm thầm, không audit** → vi phạm luật.
- **Fix:** default role-permission chỉ seed khi role **tạo mới lần đầu** (`freshlyCreatedRoleCodes`). Role đã tồn tại → giữ nguyên cấu hình admin đã sửa. Catalog permission/role vẫn upsert tên (an toàn, không phải "revert").
- **Regression (chặn tái diễn):** SELFTEST3 thêm 3 assert — admin gỡ CUSTOMER_CREATE khỏi SALES → re-seed → quyền vẫn bị gỡ (không tự cấp lại) + quyền chưa gỡ giữ nguyên. Chạy trên DB throwaway sạch: **failures=0**.

### B04 — Self-test 2/3 ghi thẳng vào dev.db khi thiếu GLB_DB_URL → nhiễm dữ liệu [FIXED]
- **Phát hiện bởi:** CMD_AUDIT — chạy SELFTEST3 lần 2 FAIL "user creates → DUPLICATE" do lần 1 đã ghi user vào dev.db (không cô lập).
- **Nguyên nhân:** `resolveDatabaseUrl` fallback về dev.db khi `GLB_DB_URL` không set; self-test 2/3 mutate DB nên chạy lặp bị nhiễm, kết quả không lặp lại được (thất bại quy trình test).
- **Fix:** guard trong `index.ts` — SELFTEST=2/3 mà thiếu `GLB_DB_URL` → **ABORT exit 2** kèm hướng dẫn (migrate deploy sang file tạm rồi trỏ GLB_DB_URL). Đã verify: abort đúng, không đụng dev.db.
- **Regression (chặn tái diễn):** **Quy trình chuẩn chạy self-test:** `rm tmp.db && DATABASE_URL=file:tmp.db prisma migrate deploy && GLB_SELFTEST=N GLB_DB_URL=file:tmp.db electron .`. Guard bắt buộc cô lập. **Bài học:** test mutate DB phải chạy trên DB dùng-một-lần, cấm ghi vào DB dev/prod.

### B05 (G-CFG-B01) — Xóa mềm ngân hàng/đối tác rồi tạo lại cùng mã → app CRASH (UnhandledPromiseRejection, IPC treo) [FIXED]
- **Phát hiện bởi:** CMD_AUDIT khi chạy GLB_SELFTEST=4 (kiểm thử 50 đúng/50 sai G-CFG) — `prisma.bank.create()` ném `P2002 Unique constraint failed (code)` **không ai bắt** → promise rejection, IPC không trả kết quả (UI sẽ treo vô hạn).
- **Nguyên nhân:** `Bank.code` và `Partner.code` là `@unique` **toàn cục ở tầng DB** (KHÔNG lọc soft-delete). Nhưng pre-check trùng mã ở service lọc `deletedAt: null` → khi một bản ghi đã xóa mềm (nằm Thùng rác) đang giữ mã đó, pre-check KHÔNG thấy → cho qua → `create()` đụng ràng buộc DB → văng. Nghịch lý với R_TRASH_RESTORE: mã bị "khóa" bởi bản ghi trong thùng rác nhưng người dùng không được cảnh báo, lại còn crash.
- **Fix (`bank-config-service.ts`):** (1) pre-check trùng mã đổi sang **quét TOÀN CỤC** (bỏ `deletedAt:null`) trong create+update của Bank & Partner; phân biệt bản đang sống → `DUPLICATE` vs bản trong thùng rác → `DUPLICATE_TRASH` ("Mã … đang nằm trong Thùng rác. Hãy phục hồi hoặc chọn mã khác."). (2) **Lưới an toàn** `isUniqueViolation(e)` bắt P2002 quanh mọi `create/update` → map về `DUPLICATE` sạch thay vì để văng (phòng race). CardType KHÔNG dính (không có DB-unique, chỉ enforce service); PartnerBank KHÔNG dính (re-link = reactivate row cũ).
- **Regression (chặn tái diễn):** SELFTEST=4 thêm 2 assert `DUPLICATE_TRASH` (tái tạo mã ngân hàng LPB + mã đối tác DT9 đã xóa mềm → chặn sạch, KHÔNG crash). Toàn bộ **109/109 PASS, exit 0, 0 UnhandledPromise**.
- **Đề xuất quy trình (bug class = "unique @DB không lọc soft-delete"):** mọi cột `@unique` trên bảng có `deletedAt` PHẢI: hoặc pre-check toàn cục + phân biệt trạng thái thùng rác, hoặc bọc P2002. CMD_AUDIT thêm bước rà: liệt kê mọi `@unique/@@unique` trên model có `deletedAt`, xác nhận service tương ứng có xử lý va chạm với bản ghi đã xóa mềm. Áp cho các module tương lai (Product/Unit/Warehouse…).


### REV-B01 — Sửa giao dịch phá snapshot phí (tái định giá theo biểu phí hiện tại) [ĐÃ CHỐT HƯỚNG FIX — Phase 1]
- **Phát hiện bởi:** CMD_AUDIT (audit Nhóm B, đọc `transaction-service.ts:244-262`). Selftest 43/43 KHÔNG bắt được (điểm mù: selftest khôi phục biểu phí về chuẩn TRƯỚC khi update → recompute trùng snapshot).
- **Nguyên nhân:** `updateTransaction` luôn gọi lại `resolveFeeForTxn` đọc biểu phí HIỆN TẠI rồi ghi đè margin/doanh thu — bất kể sửa trường gì. Sửa ghi chú của GD cũ cũng tái định giá theo phí mới → phá bất biến snapshot LEAD chốt; GD đã đối soát cũng bị đổi số đã ghi.
- **Hướng fix (LEAD 9/7 chốt qua thiết kế Phase 1):** chứng từ bill **BẤT BIẾN** — bỏ hẳn sửa bill; sai thì **hủy có lý do + duyệt Manager/Admin + tạo bill mới**. Doanh thu dẫn xuất tính theo **kỳ giá tại txnDate** (không phải giá hôm nay). Đóng triệt để.
- **Regression:** bất biến I4 (sửa note không đổi tiền — nay là "không có đường sửa bill" I17), I19 (backfill không đổi số gốc). PHẢI có ca "đổi giá kỳ RỒI thao tác" để khỏi mù như selftest cũ.

### REV-B02 — Lọc bill theo TID/đối tác/MID/HKD bỏ sót giao dịch của TID đã xóa mềm [ĐÃ CHỐT HƯỚNG FIX — Phase 1]
- **Phát hiện bởi:** CMD_AUDIT (`transaction-service.ts:324` — `buildWhere` resolve tidId chỉ lấy TID `deletedAt:null`).
- **Nguyên nhân:** GD tồn tại độc lập (khóa `tidId`), nhưng lọc lại chỉ tra TID còn sống → xóa mềm cấu hình TID làm doanh thu/công nợ của TID đó biến mất khi lọc. Vi phạm nguyên tắc "bill bất biến, độc lập vòng đời TID/serial".
- **Hướng fix:** lọc bill theo `tidId` **bất kể** TID còn sống hay đã xóa mềm. Bất biến I11.

### REV-B03 — Trang Công nợ cap 500 dòng nhưng KPI/tfoot tính tổng toàn bộ, không phân trang [ĐÃ CHỐT HƯỚNG FIX — Phase 1]
- **Phát hiện bởi:** CMD_AUDIT (`DebtPage.tsx:76,182-190` + clamp `listTransactions:387`).
- **Nguyên nhân:** bảng nạp `pageSize:500` (bị clamp), footer/KPI dùng aggregate toàn bộ → >500 GD chưa đối soát thì tổng không khớp dòng hiển thị và không có cách xem/đối soát phần vượt.
- **Hướng fix:** thêm phân trang trang Công nợ + thống kê đã/chưa thu 2 chiều (Phase 1, mục công nợ).

### B17 — Cạm bẫy tsconfig emit → `tsc -p` trần phun `.d.ts` đè `src/`, clobber `preload/index.d.ts` (1115→174 dòng, mất DTO) [FIXED]
- **Phát hiện bởi:** CMD_AUDIT (verify độc lập P1.2: web typecheck vỡ dù CMD_BUILD báo PASS). CMD_BUILD điều tra ra root cause thật.
- **Nguyên nhân:** `apps/desktop/tsconfig.node.json`+`tsconfig.web.json` đặt `declaration:true`+`composite:true`, **KHÔNG `noEmit`/`outDir`**, và `include` phủ cả `src/preload/index.d.ts` (file hand-maintained). Chạy `tsc -p` **trần (thiếu `--noEmit`)** → TS EMIT ~200 `.js/.d.ts` **đè co-located vào `src/`**, clobber file DTO hand-maintained. CẢ CMD_BUILD LẪN AUDIT đều kích hoạt khi verify bằng `tsc -p` trần. Thất bại kép: (a) bẫy build-infra, (b) lệnh verify của chính auditor không an toàn, (c) gate `npm run typecheck` cũ (`tsc -b || tsc -p tsconfig.json` — file root không tồn tại) không bắt lỗi web nên false-PASS sống được.
- **Fix:** (1) thêm `"outDir": "../../out/tsc-typecheck"` vào 2 tsconfig → emit lỡ tay rơi ra ngoài `src/`. (2) `.gitignore` thêm `apps/desktop/src/**/*.js|*.js.map|*.d.ts.map`. (3) sửa `npm run typecheck` → gate thật node+web (`--noEmit`). (4) `preload/index.d.ts` re-apply đúng cách (Edit chèn, không Write đè) → 1147 dòng, đủ DTO + Window augmentation.
- **Regression (chặn tái diễn — HARDLOCK VẬT LÝ):**
  - **Guard** `tools/audit/protected_artifacts_guard.mjs` + registry `protected_artifacts.json` (3 lớp: sàn dòng / %thu-nhỏ-vs-HEAD / anchor DTO). `npm run audit:protected`. Tự-kiểm-thử: PASS thật / FAIL clobber.
  - **Pre-commit hook** `.githooks/pre-commit` + `core.hooksPath=.githooks` → commit file clobber BỊ CHẶN (đã test).
  - **Đã tái hiện độc lập neutralize:** `tsc -p` trần → 0 rác trong `src/` (emit rơi `out/`), index.d.ts nguyên 1147.
- **Đề xuất quy trình (bug class "emit-trap" + "false-PASS"):** MỌI verify typecheck dùng `--noEmit`/`npm run typecheck`, **CẤM `tsc -p` trần**. AUDIT không tin số agent — rerun 100% gate từ trạng thái sạch. File hand-maintained vào registry được bảo vệ, chỉ Edit không Write. Chi tiết `docs/CMD_BUILD_DISPATCH_PROTOCOL.md`. Áp global mọi project TS đa-agent.

### B18 — TOCTOU/lost-update tương tranh trong approval (2 client duyệt/tạo cùng 1 yêu cầu hủy → duyệt 2 lần, audit+notify nhân đôi) [FIXED — G10.C]
- **Phát hiện bởi:** re-QA vòng 2 (chuẩn bị G10 đa-client Postgres). SQLite single-writer che được nên selftest cũ (18/19) KHÔNG bắt — điểm mù: test tuần tự không tạo race.
- **Nguyên nhân:** service layer `approval-service` đọc-rồi-ghi (read tx POSTED → tạo request + set CANCEL_PENDING; approveOne set CANCELLED) KHÔNG dùng `$transaction` + KHÔNG guard trạng thái. Lên nhiều-client (Postgres) → 2 client tạo 2 request cho 1 bill / duyệt request stale 2 lần → hỏng dữ liệu THẦM LẶNG (không crash). Storage-Guard cũng chết-lặng khi DB thành postgresql:// (CRITICAL-B, để G10.2).
- **Fix (mở lại P1.2 freeze — Mr.Long duyệt vượt tier 10/7):** bọc `requestCancelBill`/`approveOne`/`rejectOne` trong `db.$transaction` interactive + **conditional transition** `updateMany({where:{id,status:<kỳ vọng>}})` → `count===0` = thua race → `TxGuardError` (INVALID_STATE/ALREADY_DECIDED) rollback, KHÔNG ghi audit/notify. request: POSTED→CANCEL_PENDING; approve: request PENDING→APPROVED + bill CANCEL_PENDING→CANCELLED; reject: request PENDING→REJECTED + bill hoàn POSTED. Audit/notify CHỈ ở nhánh thắng. + seed pre-tạo dòng code_counter mọi prefix (chống race insert-đầu). Mã bill GD dựa SERIAL id (an toàn).
- **Regression:** selftest=18 giữ 31/0 + selftest=19 giữ 26/0 (happy-path không vỡ) + **selftest MỚI =21 guard-logic 19/0** (tạo request khi CANCEL_PENDING→INVALID_STATE không tạo request 2; duyệt 2 lần→lần 2 ALREADY_DECIDED không nhân đôi; từ chối→bill về POSTED). **Stress-race THẬT** để G10.5 trên Postgres (selftest=20).
- **Đề xuất quy trình (bug class "concurrency-masked-by-single-writer"):** mọi flow đa-ghi (đọc-rồi-ghi trạng thái) PHẢI conditional-transition + `$transaction` TRƯỚC khi lên DB đa-client. Rà toàn service layer khi migrate SQLite→Postgres. Test tuần tự KHÔNG đủ — cần stress-race trên DB đa-writer.

### B52 — Batch audit đa-agent 15/7 (bug tiền hủy phiếu hệ thống + 6 bug UI) [FIXED — 0.2.37]
- **Phát hiện bởi:** 3 subagent audit song song (backend/transaction · renderer · money-integrity) do Mr.Long yêu cầu "đào bug ẩn".
- **B52a (P1 TIỀN):** `cancelCashEntry` KHÔNG xét `sourceType` → hủy trực tiếp phiếu sinh tự động (DEVICE_DEPOSIT cọc/hoàn cọc, RENT thuê, BAD_DEBT ghi giảm nợ xấu, SALE_POS/SALE_TID doanh thu bán máy) làm **quỹ lệch tiền thật + mồ côi** DeviceDeposit/Transaction/DeviceSale. **Fix:** chỉ cho hủy `sourceType=null` (thủ công + thu công nợ, đã có hoàn đối soát M3) hoặc `SALE_COLLECT` (đã có M3b); còn lại → `SOURCE_LOCKED` (ghi audit denied). Renderer lọc luôn: DTO thêm `sourceType`, `isCancellable()` ẩn nút hủy + checkbox tích-chọn với phiếu hệ thống. **Regression:** ST26 thêm 5 assert — insert phiếu DEVICE_DEPOSIT POSTED → hủy trả SOURCE_LOCKED, phiếu vẫn POSTED, quỹ giữ nguyên 1.550.000. PASS.
- **B52b (P1 UI):** bulk-select `sel.clear()` bị QUÊN trong `reload()` (Customers/Staff/Cash) → lọc/tải lại vẫn giữ id đã ẩn → yêu-cầu-hủy nhầm hàng không nhìn thấy. **Fix:** `sel.clear()` đầu mọi `reload()`.
- **B52c (P1 UI):** nút "Đi tới Bảo trì" (cảnh báo bộ nhớ) `setActive('maintenance')` — không phải menu key → rơi về Dashboard. **Fix:** `setActive('system')`.
- **B52d (P2 UI):** bọc `<th>/<td>` thừa quanh `SelectAllCell/SelectCell` (đã tự trả cell) → DOM lồng cell không hợp lệ + lệch padding. **Fix:** render bare, nhánh non-selectable Staff giữ `<td>` thường.
- **B52e (P2 UI):** colSpan lệch — SourceTab (Dossier) 10/8 nhưng thực 7/5 sau khi khối audit rút còn 3 cột; CashEntry loading/empty cứng 9 nhưng header 8 khi thiếu quyền hủy. **Fix:** `canManage?7:5` và `canCancel?10:8`.
- **B52f (P2 security):** `captureRegion` (screenshot) nhận IPC toàn cục không validate sender + toạ độ → cửa sổ khác giả mạo kênh / crop NaN. **Fix:** chỉ nhận event từ overlay.webContents + validate số hữu hạn + clamp crop trong biên ảnh.
- **Đề xuất quy trình (bug class "helper tự-render cell nhưng caller cứng markup/colSpan" + "cancel không xét nguồn"):** (1) mọi hàm hủy/hoàn tiền PHẢI whitelist theo `sourceType`/nguồn sinh, có regression hủy-phiếu-hệ-thống-bị-chặn; (2) guard vitest/RTL: `thead th == tbody row cell == colSpan loading` mỗi tổ hợp quyền + lint cấm `<th>/<td>` bọc trực tiếp `SelectAllCell/SelectCell`; (3) `reload()` của trang bulk-select PHẢI `sel.clear()`.

### B68 — updateUser gỡ vai trò ADMIN khỏi Admin cuối → 0 admin (khóa cứng quản trị) [FIXED] — audit 16/7 (agent phản biện)
- **Phát hiện bởi:** agent phản biện săn bug ẩn (đợt 0.2.52), LEAD verify tại code (`user-service.ts` nhánh `changingRoles`). **Kịch bản:** hệ thống còn 1 Admin ACTIVE. Actor chỉ cần quyền `USER_UPDATE` (+ đủ scope gán role thường) gọi `updateUser(adminId, { roleCodes: ['SALES'] })` hoặc `roleCodes: []` → Admin cuối MẤT vai trò ADMIN → **0 admin** → không ai quản trị được nữa.
- **Nguyên nhân:** nhánh đổi TRẠNG THÁI của `updateUser` có kiểm last-admin (AUTH-02), nhưng nhánh đổi VAI TRÒ (`changingRoles`) chỉ kiểm self-escalation + manager-scope + grant-escalation, **KHÔNG** gọi `countActiveAdmins`/`canRemoveOrLockAdmin`; và không chặn `roleCodes` rỗng (createUser thì có). Đây là **bug class AUTH-05 bị sót 1 đường** (delete/lock đã vá, updateUser-role thì chưa).
- **Fix:** tính `removingAdmin = changingRoles && status ACTIVE && current có ADMIN && next KHÔNG có ADMIN`; nếu `toInactive || removingAdmin` → kiểm last-admin **và** thực thi mutation trong CÙNG `$transaction` cầm `pg_advisory_xact_lock(748301)` (M-2: chống TOCTOU song song với lock/xóa admin khác). Cấm `roleCodes` rỗng → `VALIDATION`. `invalidateAuthSnapshot()` sau đổi role (L-4).
- **Regression:** ST2 phaseb +4 assert — control (còn admin khác → CHO PHÉP) · gỡ ADMIN khỏi admin cuối → `LAST_ADMIN` · rỗng role → `VALIDATION` · status admin cuối inactive → `LAST_ADMIN`. PASS. **Đề xuất quy trình:** mọi bất biến "Admin cuối" phải phủ CẢ 3 đường (delete, lock/status, đổi-role) trong regression; khi thêm 1 nhánh mutation lên user/role, checklist bắt buộc "có chạm số Admin ACTIVE không? → cùng lock 748301".

### Gói audit kèm 0.2.52 (M-2/M-3/L-4/L-5 + Bug #1 search)
- **M-2** (đã gộp B68): kiểm last-admin của updateUser giờ dưới advisory lock 748301, hết TOCTOU.
- **M-3** — `globalSearch` ném lỗi DB thô ra renderer nếu tiện ích `unaccent` chưa cài (user Postgres thiếu quyền CREATE EXTENSION): bọc try/catch → trả `{ok:false, message}` thân thiện thay vì crash handler `search:global`. (Injection: đã an toàn — tagged-template tham số hóa.)
- **L-4** — `invalidateAuthSnapshot()` là dead code (chỉ selftest gọi): wire vào `updateUser` khi đổi vai trò → snapshot phiên hiện tại rebuild ngay thay vì chờ TTL 8s.
- **L-5** — `CashEntryDto` thiếu khai báo `sourceType` dù `toDtos` trả về: bổ sung field cho khớp type.
- **Bug #1 (UI, agent theo dõi)** — ô tìm kiếm topbar nhảy-để-lọc chỉ chạy cho POS/TID; Khách/Giao dịch/Hồ sơ nhảy đúng trang nhưng **không lọc** (DossierPage/RevenueDebtPage không nhận `initialSearch`). **Fix:** thread `jump {kind,id,term}`: Hồ sơ → DossierTab search (khớp MST/tên); Khách → RevenuePage lọc `customerId`; Giao dịch → thêm filter `code` (contains) ở `transactionList` + ô "Mã GD". Verify: typecheck 0 · ST15 108/0. **Đề xuất quy trình:** task "áp cho MỌI trang" (search-jump) phải enumerate ĐỦ đích (5 kind) + verify từng đích, không dừng ở 2/5 ([[feedback_cross_cutting_ui_completion_gate_11_7]]).
